# Local secrets (e.g. GEMINI_API_KEY for `netlify dev`) — never commit
.env
.env.*

# Netlify CLI local state
.netlify/
node_modules/

# Local Claude Code tooling: the vendored ui-ux-pro-max design skill (1.9 MB),
# machine-local settings, and progress notes. Not site content — and netlify.toml
# publishes ".", so committing this would ship it to the live site.
.claude/
