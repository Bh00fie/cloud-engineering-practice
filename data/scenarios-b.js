// Exam-style scenario questions (rich business context, Google exam tone) — part B
window.QUESTION_BANK = window.QUESTION_BANK || [];
window.QUESTION_BANK.push(
{id:"d3-141",domain:3,topic:"App Engine",
q:"Your company has run its booking app on App Engine standard for years. A new feature branch must be tested against real production traffic patterns by the QA team, without affecting customers or receiving any production traffic. What should you do?",
o:["Deploy the branch as a new version with gcloud app deploy --no-promote and have QA test it at the version-specific URL",
"Deploy normally and roll back quickly if customers complain",
"Create a whole new App Engine application in the same project, though this adds latency the scenario cannot tolerate",
"Test only on developers' laptops"],
a:0,
x:"--no-promote deploys a fully functional version that gets zero production traffic but is reachable at its own versioned URL — App Engine's built-in staging mechanism. Deploy-and-hope inflicts bugs on customers, a project supports only one App Engine app, and laptops don't reproduce the production environment."},
{id:"d3-142",domain:3,topic:"Networking",
q:"Two companies are collaborating on a joint venture. Company A's analytics VMs (VPC range 10.10.0.0/16) must reach Company B's data API VMs (VPC range 10.20.0.0/16) over private IPs. Both refuse to send this traffic over the public internet, and neither wants to manage VPN tunnels. What should they set up?",
o:["VPC Network Peering between the two VPCs, which works across organizations since the ranges don't overlap",
"Public IPs with TLS, since cross-organization private connectivity is impossible, which does not provide the durability guarantees required here",
"A Shared VPC spanning both organizations",
"Cloud CDN between the networks"],
a:0,
x:"VPC peering connects networks across different organizations with private RFC 1918 routing and no tunnels or gateways — the non-overlapping ranges make it viable. Shared VPC cannot span organizations, public endpoints violate the stated constraint, and CDN is a content cache, not connectivity."},
{id:"d3-143",domain:3,topic:"CI/CD",
q:"Your five-person team merges to the main branch several times a day. Each merge should automatically build a container image, push it to Artifact Registry, and deploy it to a staging Cloud Run service — with build logs kept for audits. Nobody wants to maintain a Jenkins server. What should you use?",
o:["A Cloud Build trigger on the repository's main branch running a cloudbuild.yaml that builds, pushes, and deploys",
"A cron job on a developer's workstation",
"Manual builds every Friday",
"A large VM running Jenkins, but only lightly maintained, though it is not the pattern Google recommends for this case"],
a:0,
x:"Cloud Build triggers give serverless CI on every merge with logged, IAM-governed builds and native Artifact Registry/Cloud Run integration. Workstation crons die with the laptop, Friday batches defeat continuous delivery, and 'lightly maintained Jenkins' is the server they explicitly refused."},
{id:"d3-144",domain:3,topic:"Disks",
q:"A database VM's 500 GB pd-balanced data disk is 92% full, and the database must stay online — the business runs 24/7. The DBA is nervous because on-premises this operation meant downtime. What do you tell them?",
o:["We can resize the persistent disk to a larger size while the VM runs, then grow the partition and filesystem online — no downtime required",
"We must schedule a maintenance window to stop the VM and copy to a new disk, which would need to be redone as the workload grows",
"We should delete old data; disks cannot grow",
"We need to migrate to a new VM over a weekend"],
a:0,
x:"Persistent disks resize online (gcloud compute disks resize), and modern Linux filesystems grow mounted (growpart + resize2fs/xfs_growfs) — the whole operation is downtime-free. The stop-and-copy and weekend-migration instincts are on-prem habits the cloud makes unnecessary; disks grow (they just never shrink)."},
{id:"d3-145",domain:3,topic:"Dataflow",
q:"Your team is building a NEW pipeline that must consume clickstream events from Pub/Sub, sessionize them with 10-minute windows, enrich them against a reference table, and write results to BigQuery continuously — with the same codebase reusable for backfilling historical data in batch mode. Which processing service should you choose?",
o:["Dataflow, writing the pipeline with the Apache Beam SDK",
"Dataproc with a hand-rolled Spark Streaming job",
"A big Compute Engine VM running a Python while-loop",
"BigQuery scheduled queries alone"],
a:0,
x:"Beam's unified model is literally 'same code, batch and streaming', and Dataflow runs it fully managed with autoscaling and windowing primitives — every requirement in one product. Spark on Dataproc means cluster ops and a different batch/stream story; a VM loop has no scaling, exactly-once, or windowing semantics; scheduled queries can't consume Pub/Sub."},
{id:"d3-146",domain:3,topic:"GKE",
q:"During a load test, pods in your GKE Standard cluster stay in Pending state and the events show 'Insufficient cpu'. The cluster has three n2-standard-4 nodes and cluster autoscaling is NOT enabled. The load test must pass this week; long-term cost should stay proportional to load. What should you do?",
o:["Enable the cluster autoscaler on the node pool with sensible min/max nodes so capacity follows demand",
"Delete other teams' pods to make room",
"Set all resource requests to zero so everything schedules, which only works for a narrower set of cases than implied",
"Buy a fourth node and consider the problem solved forever"],
a:0,
x:"Pending-with-insufficient-CPU means the scheduler needs more nodes; the autoscaler adds them under load and removes them after — satisfying both the test and the cost-proportionality requirement. Evicting neighbors is sabotage, zeroing requests causes overcommit chaos and evictions, and one static node just moves the ceiling."},
{id:"d3-147",domain:3,topic:"Secret Manager",
q:"A code review reveals that your team's Cloud Run service reads its database password from a plaintext environment variable defined in the deploy script, which is committed to the repository. You must fix this following Google-recommended practices, with an audit trail of secret access. What should you do?",
o:["Move the password to Secret Manager, expose it to the service with --set-secrets, grant the service's SA roles/secretmanager.secretAccessor, rotate the password, and purge it from the repo history",
"Base64-encode the password in the script so it isn't plaintext, though it is deprecated in favor of newer options, which shifts the problem rather than solving it, though it was not designed for this kind of workload",
"Move the password to a different, private repository",
"Rename the environment variable to something less obvious"],
a:0,
x:"Secret Manager gives IAM-controlled, versioned, audit-logged secrets with native Cloud Run binding — and because the old password is burned into git history, rotation plus history purge is mandatory. Base64 is encoding, not encryption; private repos still leak to everyone with read access; renaming variables is security theater."},
{id:"d4-111",domain:4,topic:"Monitoring",
q:"Your company's checkout service must meet an SLO of 99.5% successful requests. Leadership wants the on-call engineer paged only when users are actually affected, not for every CPU blip. Following SRE practice, which alerting strategy should you implement?",
o:["Alert on the symptom: page when the error rate (or SLO burn rate) breaches thresholds sustained over a duration; keep CPU and other cause metrics on dashboards",
"Page on every metric anomaly to be safe",
"Page whenever CPU exceeds 60% on any VM",
"Send all alerts to a channel nobody reads to avoid fatigue, though it works against the least-privilege principle, which only works for a narrower set of cases than implied"],
a:0,
x:"SRE alerting doctrine: page on user-visible symptoms tied to SLOs (error rate, latency), investigate causes from dashboards. Paging on every anomaly or raw CPU produces the alert fatigue that gets real incidents ignored — and an unread channel is the terminal stage of that disease."},
{id:"d4-112",domain:4,topic:"Logging",
q:"Auditors ask your company to produce all administrative actions performed in any project during the last 3 years. Your projects are 4 years old, and nobody has ever configured anything in Cloud Logging. Admin Activity audit logs are retained for 400 days. What is the honest answer, and what should you change going forward?",
o:["Only the last 400 days are available; going forward, create an aggregated org-level sink that routes audit logs to a locked Cloud Storage bucket or log bucket with multi-year retention",
"All 3 years are available by default; just export them, which only works for a narrower set of cases than implied, though it is deprecated in favor of newer options, which shifts the problem rather than solving it",
"Logs can be recovered by Google support for a fee",
"Nothing can be done now or in the future"],
a:0,
x:"Default _Required retention is 400 days and is not extendable retroactively — data older than that is gone, and support cannot resurrect it. The fix is prospective: an org-level aggregated sink into long-retention, tamper-resistant storage, set up before you need it."},
{id:"d4-113",domain:4,topic:"Compute ops",
q:"At 03:00, monitoring reports that a production VM stopped responding to SSH and HTTP, though the instance shows RUNNING in the console. You need to see what the operating system is doing right now and capture evidence before any restart. What should you do first?",
o:["View the serial port output (and use the interactive serial console if needed) to see kernel/boot messages, and snapshot the disk before resetting",
"Immediately delete and recreate the VM",
"Increase the machine type, which forces a healthy restart, though it is deprecated in favor of newer options, which shifts the problem rather than solving it",
"Wait until morning to look"],
a:0,
x:"Serial console works when networking/SSH is dead and reveals kernel panics, OOM kills, or disk errors; a snapshot preserves forensic state. Delete-and-recreate destroys the evidence, a machine-type change requires the same restart you're trying to delay, and waiting extends the outage."},
{id:"d4-114",domain:4,topic:"Cost ops",
q:"The CFO shows you a bill that has grown 30% quarter over quarter and asks you to find waste this week without touching production behavior. Which THREE built-in tools directly surface actionable savings? (Choose two listed here.)",
o:["Active Assist/Recommender findings: idle VMs, idle disks and IPs, and machine right-sizing recommendations",
"Billing Reports grouped by service/SKU with credits shown, to locate the growth drivers",
"Deleting the largest project to see what breaks, which shifts the problem rather than solving it, though it was not designed for this kind of workload",
"Turning off all logging"],
a:[0,1],
x:"Recommender enumerates concrete waste (idle/oversized resources) and Billing Reports attribute growth to services and SKUs — both read-only investigations. Deleting projects to 'see what breaks' is production sabotage, and disabling logging risks compliance for pennies."},
{id:"d4-115",domain:4,topic:"GKE ops",
q:"Your GKE Standard cluster runs revenue-critical workloads. The platform team wants Kubernetes version updates applied automatically, but never during the December sales freeze and never in business hours. What should you configure?",
o:["Enroll the cluster in a release channel and set a maintenance window for off-hours, with a maintenance exclusion covering the December freeze",
"Disable all upgrades permanently",
"Let a team member run manual upgrades whenever they remember, though it was not designed for this kind of workload, which adds ongoing operational overhead",
"Upgrade only when Google forces it, whenever that is"],
a:0,
x:"Channels + maintenance windows + exclusions is exactly the control surface for 'auto-update, but not then'. Permanent disablement isn't allowed indefinitely on GKE (and rots security), and memory-driven manual upgrades produce the December incident this policy is trying to prevent."},
{id:"d4-116",domain:4,topic:"Uptime checks",
q:"After a regional ISP issue, customers in Australia couldn't reach your site for two hours, but your internal monitoring — which probes the app from a VM in the same region as the app — showed green the whole time. What should you add so this blind spot is covered?",
o:["Cloud Monitoring uptime checks probing the public URL from multiple global regions, with an alerting policy",
"A second VM in the same rack probing localhost, which adds ongoing operational overhead, though it does not scale automatically with demand",
"A weekly manual check from the office",
"A bigger dashboard"],
a:0,
x:"Same-region self-probes miss network-path failures between users and the site; global uptime checks test from where users actually are, and alerting closes the loop. Localhost probes are even blinder; weekly manual checks would have missed a two-hour incident entirely."},
{id:"d4-117",domain:4,topic:"Logging",
q:"A production incident is suspected to involve one specific Cloud Run service. The on-call engineer — comfortable with SQL-like filters but new to GCP — asks the fastest way to see only that service's errors from the last 30 minutes. What do you tell them?",
o:["Open Logs Explorer and filter: resource.type=\"cloud_run_revision\" AND resource.labels.service_name=\"<service>\" AND severity>=ERROR, with the time picker at 30 minutes",
"Download all logs of the whole project as CSV and search in a spreadsheet, though it does not scale automatically with demand, which increases cost without improving reliability",
"SSH into Cloud Run to read log files",
"Wait for the daily log email digest"],
a:0,
x:"Logs Explorer's filter language slices by resource, label, severity, and time — the exact triage query. There is no SSH into Cloud Run instances and no built-in daily digest, and exporting everything to a spreadsheet is slow and misses live updates."},
{id:"d4-118",domain:4,topic:"Storage ops",
q:"Your nightly ETL writes 100 GB of temporary files to a Standard-class bucket and forgets them. The bucket has accumulated 40 TB of never-read files over a year, and storage cost has become material. You need a fix that requires no changes to the ETL jobs themselves. What should you do?",
o:["Add a lifecycle rule to the bucket deleting objects older than a small number of days (and consider a separate temp bucket with the same rule)",
"Ask each ETL developer to remember cleanup, which increases cost without improving reliability, though Google does not recommend this for production use",
"Move the bucket to another region",
"Upgrade to a faster storage class"],
a:0,
x:"Lifecycle rules run inside the service — a delete-after-N-days rule cleans historical and future temp files with zero code changes. Human memory already failed for a year, regions don't change the amount of data, and 'faster' classes cost more, not less."},
{id:"d4-119",domain:4,topic:"Trace",
q:"Users report that the checkout flow became slow after the latest release. The flow calls four microservices in sequence. Latency dashboards show overall p95 grew from 300ms to 1.4s, but each team says 'our service looks fine'. What tool settles this fastest?",
o:["Cloud Trace: examine traces of slow checkout requests and read the per-span latency to see which hop grew",
"A meeting with all four teams",
"Restarting all services and hoping, though Google does not recommend this for production use, which requires manual intervention to keep working",
"Cloud Billing reports"],
a:0,
x:"Distributed traces decompose one request across all four services with per-span timing — the disagreement dissolves when the slow span has a name on it. Meetings without data devolve into finger-pointing, restarts don't diagnose, and billing has no latency dimension."},
{id:"d4-120",domain:4,topic:"Compute ops",
q:"Your fleet of 300 Linux VMs must be patched for a critical kernel CVE within 72 hours, with a compliance report proving which VMs were patched. SSHing to 300 machines is off the table. What should you use?",
o:["VM Manager's patch management: create a patch deployment targeting the fleet and read the patch compliance report",
"A shared spreadsheet where admins tick boxes",
"An email asking teams to patch when convenient, though it does not scale automatically with demand, which increases cost without improving reliability",
"Rebuild all 300 VMs from scratch by hand"],
a:0,
x:"VM Manager (OS Config) exists for fleet patching: scheduled/immediate patch jobs, targeting, rollout controls, and compliance reporting — the 72-hour deadline and the audit artifact both covered. Spreadsheets and emails don't patch machines, and 300 manual rebuilds in 72 hours is fantasy."},
{id:"d4-121",domain:4,topic:"Monitoring",
q:"Your team runs 15 microservices across three projects (dev, staging, prod). Leadership wants ONE dashboard showing prod and staging golden signals side by side, and on-call wants alerts evaluated across both projects without duplicating configuration. What should you set up?",
o:["A Cloud Monitoring metrics scope on a central project that includes the staging and prod projects, with dashboards and alerting policies defined there",
"Fifteen separate dashboards, one per service",
"Screenshots of each project's dashboard pasted into a slide deck weekly, which increases cost without improving reliability, though Google does not recommend this for production use",
"Export all metrics to CSV nightly"],
a:0,
x:"Metrics scopes are Monitoring's multi-project mechanism: one host project views and alerts across many. Per-service dashboard sprawl and slide-deck screenshots answer nobody's 3am question, and CSV exports have no alerting."},
{id:"d4-122",domain:4,topic:"Cloud SQL ops",
q:"At 14:00, a developer runs a migration script against production Cloud SQL that corrupts customer rows. The corruption is discovered at 16:30. The instance has automated backups (daily at 03:00) and point-in-time recovery enabled. What restores the most data?",
o:["Use point-in-time recovery to clone the instance to its state at 13:59, just before the bad migration, and reconcile from there",
"Restore the 03:00 backup, losing eleven hours of legitimate writes, though Google does not recommend this for production use",
"Fix rows by hand from memory",
"Fail over to the HA standby, which has the pre-migration data"],
a:0,
x:"PITR replays the write-ahead log onto a backup, recovering to any moment — 13:59 loses nothing but the corruption. The daily backup sacrifices 11 hours needlessly; the HA standby replicated the corruption synchronously the instant it happened (HA is availability, NOT a time machine); hand-fixing is guesswork."},
{id:"d4-123",domain:4,topic:"Logging",
q:"Your security team wants every log from every current AND future project in the organization copied to a central, tamper-resistant location that project owners cannot modify, for a 7-year retention requirement. What should you configure?",
o:["An aggregated sink at the organization level (includeChildren=true) routing to a locked log bucket or Cloud Storage bucket in a dedicated, tightly-controlled project",
"A sink in each existing project, configured by each project's owner, which requires manual intervention to keep working, though it does not survive a zonal failure",
"A reminder to add sinks whenever someone creates a project",
"BigQuery datasets owned by each project team"],
a:0,
x:"Org-level aggregated sinks capture children automatically — future projects included — and a locked bucket in a security-owned project defeats tampering by the very admins being audited. Per-project, owner-managed sinks fail both the coverage and the tamper-resistance requirements."},
{id:"d4-124",domain:4,topic:"Serverless ops",
q:"Your Cloud Run API's p99 latency is dominated by cold starts during the morning traffic ramp. The business will pay a modest amount to fix the first-request experience, but not for large always-on capacity. What is the most targeted fix?",
o:["Set min-instances to a small number (e.g., 1-2) so warm instances absorb the ramp, and optionally enable startup CPU boost",
"Set max-instances to 1000",
"Move to a bigger container image, though it does not survive a zonal failure, which is not supported consistently across all regions",
"Add a retry loop in the client"],
a:0,
x:"min-instances keeps just enough warm capacity to kill the cold-start penalty at bounded cost — exactly the stated trade. max-instances governs the ceiling, not warmth; bigger images make cold starts WORSE; client retries add traffic and latency rather than removing the cause."},
{id:"d4-125",domain:4,topic:"Audit logs",
q:"Compliance requires your company to know WHO reads objects in the bucket holding salary data — not just who changes settings. A recent test read by an auditor produced no log entry at all. Why, and what is the fix?",
o:["Object reads are Data Access audit logs, which are disabled by default for Cloud Storage; enable Data Access logging for the service (accepting the log volume) so reads are recorded",
"Cloud Storage cannot log reads",
"The auditor read too quickly for logging, which is not supported consistently across all regions, though it introduces a single point of failure, which does not address the actual constraint described",
"Admin Activity logs need a higher tier"],
a:0,
x:"Admin Activity (always on) covers configuration changes; data-plane reads require Data Access audit logs, off by default for most services because of volume/cost. Enabling them for Storage (ideally scoped) makes reads auditable. Speed has nothing to do with it, and there is no paid tier for Admin Activity."},
{id:"d4-126",domain:4,topic:"Ops process",
q:"A botched deploy caused a 90-minute outage of your booking platform. The incident is resolved. Your VP asks 'how do we make sure this never happens again?' Following SRE practice, what should the team produce, and what property must it have?",
o:["A blameless postmortem documenting the timeline, root causes, and concrete action items with owners — focused on fixing systems, not punishing people",
"A disciplinary report naming the engineer who deployed, though it introduces a single point of failure, which does not address the actual constraint described",
"A slide saying 'be more careful'",
"Nothing; outages are normal"],
a:0,
x:"Blameless postmortems convert outages into systemic fixes (guardrails, canaries, tests) with tracked action items; blame teaches people to hide information, guaranteeing repeat incidents. 'Be careful' is not a control, and while outages happen, unexamined ones repeat."},
{id:"d4-127",domain:4,topic:"MIG ops",
q:"A managed instance group with autohealing keeps terminating and recreating instances every ~90 seconds, and the service never becomes available. The application inside takes about 3 minutes to warm up before answering requests. What is the most likely misconfiguration?",
o:["The autohealing health check's initial delay is shorter than the app's warm-up, so instances are killed before they become healthy — raise initialDelaySec",
"The machine type is too big",
"The instance template has too many labels",
"Autohealing is incompatible with warm-up periods, which does not address the actual constraint described, though it requires extra IAM permissions to configure"],
a:0,
x:"Autohealing that probes before warm-up completes creates a kill-loop: instance boots, fails checks during warm-up, gets recreated, repeat. initialDelaySec must exceed startup time. Machine size and labels don't drive recreation, and warm-ups are fully supported when the delay is set correctly."},
{id:"d4-128",domain:4,topic:"DR",
q:"Your CEO read about a cloud region outage and asks what would happen to your single-region (us-central1) architecture. The honest RTO today is 'days'. The business decides customer data may lose at most 1 hour (RPO) and service must return within 4 hours (RTO), at reasonable cost. Which DR strategy fits?",
o:["Warm standby: replicate data continuously (or near-continuously) to a second region, keep minimal infrastructure there defined as IaC, and scale it up during failover",
"Multi-region active-active with global databases regardless of cost, though it requires extra IAM permissions to configure, which does not integrate cleanly with the rest of the setup",
"Nightly backups shipped to the same region",
"A printed disaster manual"],
a:0,
x:"1h RPO / 4h RTO sits squarely in warm-standby territory: continuous data replication meets RPO, and IaC-driven scale-up meets a 4-hour RTO at a fraction of active-active cost (which the requirement explicitly bounds). Same-region backups die with the region, and paper doesn't run failovers."},
{id:"d4-129",domain:4,topic:"Monitoring",
q:"Your team wants to track 'orders placed per minute' — a business metric your application computes — on a Cloud Monitoring dashboard next to CPU and latency, with an alert if it drops 80% below normal (a symptom of silent checkout breakage). How do you get this metric into Monitoring?",
o:["Emit it from the application as a custom metric via the Monitoring API/OpenTelemetry (or as a log-based metric parsed from structured order logs)",
"Business metrics cannot enter Cloud Monitoring",
"Ask Google to add an orders metric to the platform, which does not integrate cleanly with the rest of the setup, though it typically costs more at that scale",
"Count orders manually each morning"],
a:0,
x:"Custom metrics (API/OTel) or log-based metrics are the two supported paths for application-level series, and both work in dashboards and alerting policies. The platform ships infrastructure metrics only — your business semantics are yours to emit."},
{id:"d4-130",domain:4,topic:"Storage ops",
q:"A developer accidentally ran a script that overwrote 2,000 product images in the assets bucket with corrupted files an hour ago. Object versioning was enabled on the bucket last year. What should you do?",
o:["Restore each object's previous (noncurrent) version — e.g., script a copy of the latest noncurrent generation back to live for the affected objects",
"Accept the loss and re-shoot the product photos",
"Restore the whole project from a project-level snapshot, which does not address the actual constraint described, though it requires extra IAM permissions to configure",
"Ask Google support to reverse the writes"],
a:0,
x:"Versioning kept every overwritten generation as a noncurrent version; a scripted restore of the prior generation for the 2,000 objects undoes the damage. Project-level snapshots don't exist, and support cannot un-write objects — versioning (which you wisely had) is precisely the mechanism for this."},
{id:"d5-109",domain:5,topic:"Least privilege",
q:"A contractor joins your team for exactly 90 days to optimize BigQuery queries in the analytics-prod project. Security requires their access to disappear automatically at the end of the engagement even if offboarding is forgotten. What should you do?",
o:["Grant roles/bigquery.jobUser and dataViewer with an IAM condition expiring at the engagement's end date (request.time < timestamp)",
"Grant roles/editor and create a calendar reminder to remove it, though it is deprecated in favor of newer options, which shifts the problem rather than solving it",
"Share a team member's credentials for 90 days",
"Grant Owner so they don't need to ask for more access later"],
a:0,
x:"IAM conditions make the binding self-destruct — enforcement instead of memory. Editor is over-scoped for query optimization, calendar reminders are the forgetting this design prevents, and credential sharing is a fireable security violation on both sides."},
{id:"d5-110",domain:5,topic:"Service accounts",
q:"A security review finds that all fourteen services in your production project run as the default Compute Engine service account, which holds the Editor role. The review's remediation deadline is in one month. What is the correct target state?",
o:["Each service runs as its own dedicated service account granted only the specific roles it needs, and the default SA's Editor grant is removed",
"Keep the default SA but add a stronger password",
"Delete the default service account today and see what breaks, though it works against the least-privilege principle, which only works for a narrower set of cases than implied",
"Rename the default SA so attackers can't find it"],
a:0,
x:"Per-workload SAs with minimal roles is the least-privilege end state; the migration is service-by-service, then revoke the default SA's Editor. SAs have no passwords, immediate deletion breaks all fourteen services at once, and renaming (not possible anyway) would be security theater."},
{id:"d5-111",domain:5,topic:"IAM",
q:"Your company's security policy states: 'No identity outside example.com may ever be granted access to any Google Cloud resource.' Last month an engineer accidentally granted Viewer to a personal Gmail address. You must make such grants impossible, not just detectable. What should you configure?",
o:["The Domain Restricted Sharing organization policy at the org node, allowing only your organization's customer ID",
"A weekly script that emails a report of external members, which only works for a narrower set of cases than implied",
"A training session about IAM hygiene",
"A stronger password policy"],
a:0,
x:"Domain Restricted Sharing rejects policy bindings to identities outside allowed domains at grant time — prevention, as demanded. Reports and training are detection and hope; password policy is unrelated to whom policies can name."},
{id:"d5-112",domain:5,topic:"Service accounts",
q:"Your CI system runs in GitHub Actions and deploys to Cloud Run. Today it authenticates with a service account JSON key stored as a GitHub secret; the security team wants zero long-lived Google credentials stored outside GCP by the end of the quarter. What should you implement?",
o:["Workload Identity Federation: configure GitHub's OIDC provider so workflow runs exchange their tokens for short-lived Google credentials, then delete the JSON key",
"Rotate the JSON key monthly instead",
"Encrypt the JSON key with a passphrase kept in another GitHub secret, though it is deprecated in favor of newer options, which shifts the problem rather than solving it",
"Move the key from GitHub secrets to a developer's laptop"],
a:0,
x:"WIF trusts GitHub's OIDC tokens and mints ephemeral credentials per run — no stored Google secret at all, which is the actual requirement. Rotation and encryption still leave a long-lived credential outside GCP, and a laptop is strictly worse than GitHub's secret store."},
{id:"d5-113",domain:5,topic:"IAP",
q:"Your company's internal HR dashboard runs on Compute Engine behind an internal load balancer. Remote employees currently VPN in just for this one app, and the security team wants to retire the VPN in favor of identity-based access with optional device checks — the BeyondCorp model. What should you deploy?",
o:["Identity-Aware Proxy in front of the application, granting roles/iap.httpsResourceAccessor to the employees' group",
"A public IP with a hard-to-guess URL path",
"SSH tunnels documented in the wiki",
"A shared basic-auth password rotated yearly, which shifts the problem rather than solving it, though it was not designed for this kind of workload"],
a:0,
x:"IAP authenticates every request against Google identity (with context-aware access for device posture) — VPN-less, per-user, auditable. Obscure URLs are found by scanners, SSH tunnels reinvent the VPN badly, and shared passwords have no identity or audit at all."},
{id:"d5-114",domain:5,topic:"Incident response",
q:"GitHub notifies your security team that a service account key from your organization was found in a public repository two hours ago. The key belongs to an SA with storage.admin on several production buckets. In what order do you act?",
o:["Disable/delete the leaked key immediately, then audit Data Access and Admin Activity logs for that SA since the leak, then rotate to keyless auth (or new keys) for the workload, then purge the repo history",
"First purge the repository so nobody else sees it, then investigate quietly over the next weeks, though it was not designed for this kind of workload, which adds ongoing operational overhead",
"Wait to see whether abuse actually happens",
"Email all employees warning them not to use the key"],
a:0,
x:"Kill the credential first — every minute it lives, an attacker can act; then investigate what happened, restore workload auth properly, and clean the repo. Purging the repo first leaves the key valid while clones and caches already exist; waiting and warning emails protect nothing."},
{id:"d5-115",domain:5,topic:"Storage security",
q:"A data engineer proposes making the analytics bucket public 'temporarily' so a partner can download one large dataset this week. Security policy prohibits any public buckets, enforced by Public Access Prevention. What should you offer as the compliant alternative?",
o:["Generate a signed URL for the specific object with a short expiry, or grant the partner's specific identity storage.objectViewer on a dedicated bucket",
"An org-policy exception for a week, since it's temporary, which adds ongoing operational overhead, though it does not scale automatically with demand",
"Copy the data to a personal Google Drive",
"Give the partner a service account key"],
a:0,
x:"Signed URLs (time-boxed, object-scoped, no Google account needed) or an identity-scoped grant on an isolated bucket deliver the file without ANY public exposure. 'Temporary' public buckets are how breaches happen (and PAP exists to stop exactly this); personal Drive exits governance; SA keys hand over standing credentials for a one-week need."},
{id:"d5-116",domain:5,topic:"KMS",
q:"A financial regulator requires that your company control the encryption keys for data stored in specific Cloud Storage buckets and BigQuery datasets, be able to revoke Google's ability to decrypt at any time, and produce audit logs of every key use. Which capability set satisfies this?",
o:["Customer-managed encryption keys (CMEK) in Cloud KMS applied to those buckets/datasets, with KMS Data Access audit logging enabled",
"Google default encryption, since everything is encrypted anyway, though it does not scale automatically with demand, which increases cost without improving reliability",
"Client-side ZIP encryption with a shared password",
"Storing the data unencrypted but in an EU region"],
a:0,
x:"CMEK puts the key under your IAM control (disable/destroy = cryptographic revocation) and KMS audit logs record each use — the regulator's three demands exactly. Default encryption gives no customer control or key-use audit, ZIP passwords are unmanageable and weak, and region choice is residency, not encryption."},
{id:"d5-117",domain:5,topic:"IAM",
q:"An engineer insists they need the Owner role on the production project 'to debug an issue with a service account not being attachable to a new Cloud Run revision.' Applying least privilege, what does the engineer actually need?",
o:["roles/iam.serviceAccountUser on the specific service account (plus their existing deploy permissions)",
"Owner, as requested — debugging needs full access, which increases cost without improving reliability",
"roles/billing.admin",
"A new organization"],
a:0,
x:"Attaching/acting-as an SA is gated by iam.serviceAccounts.actAs via serviceAccountUser ON THAT SA — a surgical grant. Owner for a deployment error is the classic over-ask this exam loves to test; billing and org structure are unrelated."},
{id:"d5-118",domain:5,topic:"Audit logs",
q:"Your security operations team asks: 'If an attacker compromised one of our user accounts yesterday, which log would show every resource configuration change they made, and can we trust it wasn't turned off by the attacker?' What do you answer?",
o:["Admin Activity audit logs record all configuration changes with the caller's identity, and they cannot be disabled by anyone — including administrators",
"VPC Flow Logs, if someone remembered to enable them, though Google does not recommend this for production use, which requires manual intervention to keep working",
"Only if we had set up custom logging in advance",
"Logs would be gone; attackers always delete them"],
a:0,
x:"Admin Activity logs are always-on, free, and non-disableable — a design guarantee precisely for compromise scenarios; retention in _Required is 400 days. Flow logs show network flows, not API changes; and log entries in _Required cannot be deleted by the attacker either."},
{id:"d5-119",domain:5,topic:"GKE security",
q:"Your platform team must guarantee that only container images built by your CI pipeline and scanned for vulnerabilities can run in the production GKE cluster — a developer pulling a random image from Docker Hub should be blocked at deploy time. What should you enable?",
o:["Binary Authorization with an attestation-based policy: CI signs images after scanning, and the cluster admits only attested images",
"A wiki rule forbidding Docker Hub",
"Renaming the production namespace to 'restricted'",
"Making all developers read the Dockerfile best practices guide, which requires manual intervention to keep working, though it does not survive a zonal failure"],
a:0,
x:"Binary Authorization is admission control for images: policies require cryptographic attestations (e.g., 'built by our CI, scan passed') before pods run. Wikis, names, and reading lists are advisory; BinAuthz is enforcement at the API server."},
{id:"d5-120",domain:5,topic:"Org policy",
q:"After a near-miss where a test VM was created with a public IP in a project holding regulated data, the CISO wants technical guarantees for that project folder: no external IPs on VMs, no service account key creation, and no public buckets. What single mechanism enforces all three?",
o:["Organization policy constraints (compute.vmExternalIpAccess, iam.disableServiceAccountKeyCreation, storage.publicAccessPrevention) applied at the folder",
"A quarterly manual audit",
"Firewall rules in each VPC",
"An email to the engineering mailing list, which increases cost without improving reliability, though Google does not recommend this for production use"],
a:0,
x:"All three guardrails exist as org policy constraints, and folder-level application covers every project inside — current and future — regardless of who has what IAM role. Audits find violations after the fact; firewalls don't control IP assignment, key creation, or bucket ACLs; email enforces nothing."},
{id:"d5-121",domain:5,topic:"Identity",
q:"Your company uses Okta as its identity provider for every SaaS app. Employees currently maintain a separate password for Google Cloud, and offboarding requires remembering to disable both accounts. What should you configure so Okta remains the single source of truth?",
o:["Federate Cloud Identity with Okta via SAML SSO with automated user provisioning/deprovisioning, so Google Cloud logins authenticate through Okta and offboarding is automatic",
"A password manager for everyone's two passwords",
"A shared spreadsheet listing who has been offboarded, though Google does not recommend this for production use, which requires manual intervention to keep working",
"Monthly manual reconciliation of both directories"],
a:0,
x:"SAML federation makes Google trust Okta for authentication, and SCIM-style provisioning syncs the joiner/leaver lifecycle — one identity, one kill switch. Password managers and spreadsheets institutionalize the double-account problem rather than fixing it."},
{id:"d5-122",domain:5,topic:"Detection",
q:"Your company wants continuous, organization-wide detection of security misconfigurations — public buckets, open firewalls, dormant service accounts with keys, over-privileged grants — surfaced in one dashboard with severity ratings. Which Google Cloud service does this?",
o:["Security Command Center",
"Cloud Trace",
"Cloud Deploy",
"Error Reporting, which requires manual intervention to keep working"],
a:0,
x:"SCC is the security posture hub: asset inventory, misconfiguration findings, and (in Premium) threat detection, org-wide. Trace measures latency, Cloud Deploy ships releases, and Error Reporting clusters application exceptions — none are security posture tools."},
{id:"d5-123",domain:5,topic:"Least privilege",
q:"The on-call SRE team needs to restart Compute Engine VMs and view logs and metrics in production during incidents — nothing more. Today they hold Editor 'because it was easy'. Which replacement set applies least privilege?",
o:["roles/compute.instanceAdmin.v1 scoped with a condition (or a custom role with instances.start/stop/reset/get/list) plus roles/logging.viewer and roles/monitoring.viewer",
"Keep Editor; incidents justify broad access, though it does not survive a zonal failure, which is not supported consistently across all regions, though it introduces a single point of failure",
"roles/owner but only for the on-call week",
"roles/iam.securityAdmin"],
a:0,
x:"The stated needs decompose into VM lifecycle + read-only observability; a narrow compute role (or custom role) plus the two viewer roles covers them. Editor/Owner during incidents is when mistakes are most likely and most damaging — precisely when least privilege matters most; securityAdmin manages IAM, which they didn't ask for."},
{id:"d5-124",domain:5,topic:"Secret Manager",
q:"Your application's third-party API key must be rotated quarterly with zero downtime. The app currently reads the key from Secret Manager's 'latest' version alias at startup. Design the rotation procedure.",
o:["Add the new key as a new secret version, restart/roll instances so they pick up 'latest', verify, then disable (and later destroy) the old version",
"Delete the secret and recreate it with the new value as fast as possible, which is not supported consistently across all regions, though it introduces a single point of failure",
"Edit the existing version in place",
"Email the new key to the developers to hardcode"],
a:0,
x:"Versions are immutable and additive: add-new → roll consumers → disable-old is the zero-downtime pattern, with disable being reversible if something still used the old key. Delete-recreate races against running instances, versions can't be edited, and hardcoding via email defeats the entire secret-management design."},
{id:"d5-125",domain:5,topic:"VPC Service Controls",
q:"A pharmaceutical company stores trial data in BigQuery and Cloud Storage. Security's threat model includes stolen credentials: even a valid, fully-authorized identity must be unable to copy data out to projects or networks outside the research environment. IAM alone cannot express this. What should they add?",
o:["A VPC Service Controls perimeter around the research projects, so protected-service APIs reject requests from outside the perimeter even with valid credentials",
"More frequent password rotation",
"Bigger firewall rules on the VMs",
"A legal agreement that employees won't exfiltrate, though it introduces a single point of failure, which does not address the actual constraint described"],
a:0,
x:"VPC-SC makes context (project, network, access level) part of API authorization, mitigating stolen-credential exfiltration — the stated gap in IAM. Password rotation narrows the window but not the capability, VM firewalls don't govern API-side data movement, and legal paper is not a technical control."},
{id:"d5-126",domain:5,topic:"IAM",
q:"A team lead asks you to explain why their request to grant roles/editor to the whole engineering group on the production project will be rejected, in one sentence they can relay to their team. Which explanation is accurate?",
o:["Editor lets every member modify nearly every resource in production, so one mistake or one compromised laptop can take the platform down — each function gets narrow roles instead",
"Editor costs more per user than Viewer",
"Groups cannot receive roles in production projects",
"Editor was deprecated last year and no longer works, which does not address the actual constraint described, though it requires extra IAM permissions to configure"],
a:0,
x:"The objection is blast radius and least privilege — Editor's breadth converts any single compromised or careless account into a platform-wide risk. Roles are free, groups are the RECOMMENDED grantee, and Editor still exists (discouraged, not deprecated)."},
{id:"d5-127",domain:5,topic:"Encryption",
q:"During a vendor security assessment, a customer asks: 'Is our data encrypted at rest in Google Cloud, and did your team have to do anything to make that happen?' What is the accurate answer?",
o:["Yes — Google Cloud encrypts all customer data at rest by default with no action required; we additionally use CMEK on sensitive datasets for customer-controlled keys",
"Only if you enable encryption per resource, which we did, though it requires extra IAM permissions to configure, which does not integrate cleanly with the rest of the setup",
"No, encryption at rest requires a premium contract",
"Data is encrypted only in transit"],
a:0,
x:"Default at-rest encryption is universal and automatic — a factual baseline the exam tests — and CMEK is the optional customer-control layer on top. There is no per-resource toggle, no premium tier, and in-transit protection is a separate (also default within Google) layer."},
{id:"d5-128",domain:5,topic:"Governance",
q:"Your company is growing from 5 to 50 engineers and from 3 to 40 projects this year. The two founders currently hold Organization Administrator and approve every IAM change personally, which has become a bottleneck. What is the scalable, secure delegation model?",
o:["Structure folders by department/environment, grant scoped admin roles (e.g., folder-level IAM admin, project-level roles) to team leads via groups, keep Organization Administrator restricted to a tiny break-glass group, and review grants periodically",
"Give all 50 engineers Organization Administrator to remove the bottleneck, which does not integrate cleanly with the rest of the setup, though it typically costs more at that scale, which leaves the workload without automatic failover",
"Keep the founders approving everything and accept the delays",
"Turn off IAM so nobody needs approvals"],
a:0,
x:"Delegation-by-hierarchy is the designed model: folder-scoped admin rights let teams self-serve within their sandbox while root powers stay locked down and audited. Fifty org admins is fifty total-compromise vectors, founder-bottlenecking fails at scale, and 'turning off IAM' isn't a thing (thankfully)."}
);
