// Exam-style scenario questions (rich business context, Google exam tone) — part A
window.QUESTION_BANK = window.QUESTION_BANK || [];
window.QUESTION_BANK.push(
{id:"d1-111",domain:1,topic:"Billing",
q:"Your company, a retail startup, has three projects: one for development, one for staging, and one for production. The finance department wants to receive an email whenever the production project's spending is forecasted to exceed €5,000 in a month, but they do not want notifications for the other projects. You need to configure this following Google-recommended practices. What should you do?",
o:["Create a budget on the billing account scoped to the production project only, set a €5,000 amount, and add a forecasted-spend threshold rule with the finance team's email as a notification channel",
"Create three separate billing accounts, one per project, and set a budget only on the production billing account",
"Export billing data to BigQuery and ask finance to run a query every morning",
"Set a quota of €5,000 on the production project's Compute Engine API"],
a:0,
x:"Budgets can be scoped to specific projects and can alert on forecasted (not just actual) spend, with Monitoring notification channels for arbitrary recipients — no restructuring needed. Splitting billing accounts adds administrative overhead for no benefit, BigQuery export is analysis rather than alerting, and quotas limit resource usage, not currency amounts."},
{id:"d1-112",domain:1,topic:"Resource hierarchy",
q:"You are the cloud engineer for a company that is onboarding to Google Cloud. The security team requires that developers must never be able to create service account keys in any current or future project, with no exceptions. You need to implement this requirement with the least ongoing administrative effort. What should you do?",
o:["Apply the iam.disableServiceAccountKeyCreation organization policy constraint at the organization level",
"Remove the iam.serviceAccountKeys.create permission from every developer's role in each project",
"Send a policy document to all developers and audit compliance quarterly",
"Create a Cloud Run function that deletes new keys every hour"],
a:0,
x:"An org policy constraint at the organization node is enforced automatically on every existing and future project — set once, applies forever. Editing roles per project is ongoing toil that misses new projects, policy documents are not enforcement, and delete-after-the-fact automation leaves a window of exposure."},
{id:"d1-113",domain:1,topic:"Projects",
q:"Your team has been experimenting in a project named sandbox-experiments for six months. The project is no longer needed, but your manager is worried that someone might still depend on a resource inside it. You want to shut it down in a way that can be reversed if a dependency surfaces. What should you do?",
o:["Shut down the project with gcloud projects delete; it can be restored for approximately 30 days before permanent deletion",
"Delete each resource one at a time so the project stays recoverable forever",
"Remove all IAM bindings so nobody can use it, and keep paying for the resources",
"Detach the billing account, which archives the project permanently"],
a:0,
x:"Project deletion enters a ~30-day pending state during which gcloud projects undelete restores everything — exactly the reversible shutdown wanted. Per-resource deletion is NOT recoverable, removing IAM keeps costs running, and detaching billing breaks services without archiving anything."},
{id:"d1-114",domain:1,topic:"gcloud",
q:"You administer resources in two projects: web-prod and web-dev, each in a different region. You switch between them many times a day from your workstation and keep making mistakes by running commands against the wrong project. Following Google-recommended practices, what should you do?",
o:["Create two named gcloud configurations, each with its own default project and region, and switch with gcloud config configurations activate",
"Open two browser tabs with different console themes",
"Always type the full resource URL in every command",
"Install the Cloud SDK twice in different directories"],
a:0,
x:"Named configurations bundle project, account, and region defaults into switchable profiles — the designed solution for multi-project work. Console tabs don't affect the CLI, full URLs are error-prone typing, and duplicate SDK installs share credentials anyway."},
{id:"d1-115",domain:1,topic:"IAM basics",
q:"A new data analyst joined your company today. She needs to view (but not modify) all Compute Engine and Cloud Storage resources in the analytics-prod project. Twenty more analysts with identical needs will join over the next year. You want to follow Google-recommended practices while minimizing future work. What should you do?",
o:["Create a Google group for analysts, grant it roles/compute.viewer and roles/storage.objectViewer on the project, and add each analyst to the group as they join",
"Grant each analyst roles/viewer individually as they join",
"Grant the first analyst roles/owner so she can onboard the others",
"Create one shared analyst Google account for the whole team"],
a:0,
x:"Group-based grants mean onboarding is just group membership — no IAM edits — and the roles are scoped to what analysts actually need. Per-user grants create 21 bindings to audit, Owner for onboarding is a massive over-grant, and shared accounts destroy accountability."},
{id:"d1-116",domain:1,topic:"Billing",
q:"Your company's finance team needs to analyze twelve months of historical Google Cloud spending by project, service, and label in their BI tool, which reads from BigQuery. Billing export to BigQuery has never been configured. What should you tell them?",
o:["You will enable billing export now, but it only captures cost data from enablement onward — the twelve months of history will have to come from billing reports or invoice exports instead",
"Enabling billing export will automatically backfill the last twelve months into BigQuery",
"BigQuery cannot store billing data; they must use spreadsheets",
"Historical data can be recovered by contacting Google support"],
a:0,
x:"Billing export starts flowing when enabled and never backfills — a classic gotcha, and the reason to enable it on day one. The console's billing reports do cover history for manual analysis, but the BigQuery dataset will only grow from today. Support cannot inject historical export data."},
{id:"d1-117",domain:1,topic:"APIs",
q:"A developer on your team writes a script that creates Compute Engine VMs in a brand-new project. The script fails with the error 'Compute Engine API has not been used in project 12345 before or it is disabled.' The developer has the Editor role. What should you do?",
o:["Enable the Compute Engine API in the project with gcloud services enable compute.googleapis.com and rerun the script",
"Grant the developer the Compute Admin role",
"Link a second billing account to the project",
"Wait, because new projects take 24 hours to activate APIs"],
a:0,
x:"New projects have most APIs disabled; the error text names the problem exactly. IAM roles don't matter while the API itself is off, an extra billing account is meaningless, and there is no 24-hour activation period — enablement takes seconds."},
{id:"d1-118",domain:1,topic:"Organization",
q:"Your startup has been creating projects under individual employees' personal Gmail accounts. An employee who owns two production projects is leaving next month. You want to prevent this situation from ever happening again. What should you do first?",
o:["Set up Cloud Identity for your company domain, create an organization, and migrate the projects into it so ownership is corporate rather than personal",
"Ask the departing employee to share their Gmail password",
"Clone all resources into brand-new projects every time someone leaves",
"Buy Google Workspace licenses for every employee's personal account"],
a:0,
x:"Cloud Identity (free) creates managed corporate identities and the organization node, making projects company assets that survive departures. Password sharing violates every security norm, resource cloning is disruptive toil, and Workspace licenses can't attach to personal Gmail accounts."},
{id:"d1-119",domain:1,topic:"Quotas",
q:"Your company plans a marketing campaign in six weeks that is expected to triple traffic. Capacity modeling shows you will need 400 vCPUs in us-central1, but the project's current CPU quota in that region is 72. The campaign date cannot move. What should you do?",
o:["Request a quota increase for us-central1 CPUs now, since increases are reviewed by Google and can take time, and verify approval well before the campaign",
"Do nothing; quotas raise themselves automatically under load",
"Create 10 new projects the week of the campaign and spread VMs across them",
"Switch all VMs to Spot to bypass the quota"],
a:0,
x:"Quota increases are human/automated-reviewed requests with lead time — exactly the thing to file early for a hard deadline. Quotas never auto-raise, project sharding at the last minute multiplies operational risk, and Spot capacity consumes quota too (and can be preempted mid-campaign)."},
{id:"d1-120",domain:1,topic:"Billing IAM",
q:"Your company hires an external accounting firm to review cloud spending each quarter. The firm must see costs, credits, and invoices for the billing account but must not be able to change anything or see any application data. Following the principle of least privilege, what should you grant?",
o:["Billing Account Viewer on the billing account to a group containing the firm's users",
"Billing Account Administrator on the billing account",
"Viewer on every project linked to the billing account",
"Owner on one project so they can 'see everything from there'"],
a:0,
x:"Billing Account Viewer is read-only cost/invoice visibility with zero resource access — the textbook auditor grant. Administrator can modify payment settings; project Viewer exposes application resources while missing consolidated billing; Owner is wildly excessive."},
{id:"d2-101",domain:2,topic:"Compute choice",
q:"Your company is migrating a monolithic e-commerce application. The team has containerized it, traffic is spiky (near zero at night, high at lunchtime), the app is stateless and HTTP-based, and there are no engineers available for infrastructure operations. You need the most cost-effective option that requires the least management. What should you do?",
o:["Deploy the container to Cloud Run with autoscaling, letting it scale to zero at night",
"Create a GKE Standard cluster with three always-on nodes",
"Run the container on two e2-standard-8 VMs behind a load balancer",
"Deploy to App Engine flexible environment"],
a:0,
x:"Stateless + HTTP + containerized + spiky + no ops team is the canonical Cloud Run profile: per-request billing and scale-to-zero directly answer the cost requirement. GKE and VMs bill for idle nights and need operations; App Engine flexible cannot scale to zero and deploys slowly."},
{id:"d2-102",domain:2,topic:"Storage classes",
q:"A media company must keep raw video footage for seven years to satisfy its content licensing contracts. Footage older than 30 days is accessed at most once or twice a year for legal disputes. The library grows by 40 TB per month, and management wants the lowest possible storage cost. What should you recommend?",
o:["Store new footage in Standard storage and use a lifecycle rule to transition objects older than 30 days to Archive class",
"Keep everything in Standard storage for access speed",
"Store everything in Nearline from day one",
"Burn the footage to physical tapes stored in the office"],
a:0,
x:"Hot for the working window, Archive (accessed less than yearly, 365-day minimum) afterward — the lifecycle rule automates the transition at 40 TB/month scale. All-Standard costs several times more; Nearline suits monthly access, not yearly; office tapes trade GCP cost for operational and durability risk the contract likely forbids."},
{id:"d2-103",domain:2,topic:"Database choice",
q:"Your gaming company is launching a title expected to reach players in Asia, Europe, and the Americas simultaneously. The player-inventory service needs a relational schema, strong consistency for trades between players in different regions, 99.999% availability, and the ability to scale writes horizontally as the player base grows. Which database should you choose?",
o:["Cloud Spanner in a multi-region configuration",
"Cloud SQL for PostgreSQL with cross-region read replicas",
"Firestore in Native mode",
"Memorystore for Redis in each region"],
a:0,
x:"Relational + strongly consistent + multi-region writes + five-nines is the exact requirement set Spanner was built for. Cloud SQL has a single write primary (replicas are read-only and asynchronous), Firestore isn't relational and has different consistency/scaling characteristics for this pattern, and Redis is a volatile cache."},
{id:"d2-104",domain:2,topic:"Networking",
q:"Your company's data center hosts a legacy licensing server that cloud VMs must reach over private IP. The connection needs about 500 Mbps, must be encrypted in transit over the public internet, and must be live within two weeks. The networking team asks what to provision on the Google side. What should you recommend?",
o:["HA VPN with two tunnels and a Cloud Router for BGP",
"Dedicated Interconnect with a 10 Gbps circuit",
"Cloud NAT",
"Making the licensing server publicly routable with a firewall"],
a:0,
x:"HA VPN provisions in hours, is IPsec-encrypted over the internet, comfortably carries 500 Mbps, and gets a 99.99% SLA with BGP dynamic routing. Dedicated Interconnect takes weeks-to-months of physical provisioning and isn't encrypted by default; Cloud NAT is outbound-only translation; exposing the server publicly contradicts the private-IP requirement."},
{id:"d2-105",domain:2,topic:"Compute choice",
q:"A research team runs nightly genomics batch jobs that take four hours on 64-vCPU machines. Jobs checkpoint every five minutes and restart cleanly from the last checkpoint. The team's budget is under pressure, and the jobs have no deadline other than 'done by morning'. What should you recommend?",
o:["Run the workers on Spot VMs, restarting from checkpoints when instances are preempted",
"Buy a 3-year committed use discount for 64 vCPUs",
"Run the jobs on the largest shared-core machines",
"Move the jobs to Cloud Run functions"],
a:0,
x:"Checkpointed, deadline-tolerant batch work is the perfect Spot profile — 60-91% savings and preemptions only cost a five-minute replay. A 3-year CUD for 4 hours/night of usage overpays massively; shared-core machines can't deliver 64 vCPUs; classic serverless functions aren't built for multi-hour 64-vCPU compute."},
{id:"d2-106",domain:2,topic:"Storage",
q:"Your web application lets users upload profile photos, which must then be served worldwide with low latency. Photos are public, average 500 KB, and are read thousands of times more often than written. The team wants minimal operational burden. What should you build?",
o:["Store the photos in a Cloud Storage bucket and serve them through an external Application Load Balancer with Cloud CDN enabled",
"Store the photos on a persistent disk attached to a serving VM in one region",
"Store the photos as BLOB columns in Cloud SQL",
"Store the photos in Filestore and mount it on web servers everywhere"],
a:0,
x:"Object storage + CDN is the canonical read-heavy public media pattern: durable, serverless, cached at the edge worldwide. A single VM's disk is a regional bottleneck and single point of failure, databases are a poor and expensive blob store at this read volume, and Filestore is regional NFS, not a global delivery mechanism."},
{id:"d2-107",domain:2,topic:"VPC planning",
q:"Your enterprise has a central networking team and twelve application teams, each with its own project. Security mandates that all subnets, routes, and firewall rules are controlled exclusively by the networking team, while application teams deploy VMs into those subnets from their own projects. What should you implement?",
o:["A Shared VPC with the networking team's project as host and the application projects attached as service projects",
"Twelve independent VPCs connected with VPC peering in a full mesh",
"One project for the entire company so there's only one VPC",
"Cloud VPN tunnels between every pair of projects"],
a:0,
x:"Shared VPC is purpose-built for centralized network administration with delegated compute: the host project owns the network, service projects consume it. A 12-way peering mesh (66 peerings) decentralizes firewall control, a single mega-project destroys IAM/quota isolation, and VPNs between projects in the same org is needless complexity."},
{id:"d2-108",domain:2,topic:"Database choice",
q:"An IoT platform ingests temperature readings from 2 million sensors, each reporting every 10 seconds — roughly 200,000 writes per second sustained. Dashboards query the latest values and short time ranges by sensor ID. The team evaluated Cloud SQL and found it cannot keep up. What should you use instead?",
o:["Bigtable, with the sensor ID and timestamp composed into the row key",
"A larger Cloud SQL instance with 96 vCPUs",
"Firestore in Native mode with one document per reading",
"Cloud Storage, one object per reading"],
a:0,
x:"Sustained hundreds of thousands of writes per second with key-range reads is Bigtable's home turf; row-key design (sensor#timestamp) makes the dashboard queries efficient scans. Vertical Cloud SQL scaling hits a ceiling well below this rate, Firestore throttles at this write volume and costs far more per operation, and millions of tiny objects per second is an anti-pattern for GCS."},
{id:"d2-109",domain:2,topic:"Regions & zones",
q:"A European bank is deploying a customer-facing application. Regulators require all customer data to remain physically within Germany, and the risk team requires the application to survive the failure of any single data center. What is the correct deployment approach?",
o:["Deploy across multiple zones of a German region (europe-west3 or europe-west10) and enforce location with an organization policy",
"Deploy across europe-west3 (Frankfurt) and europe-west1 (Belgium) for geographic diversity",
"Use an EU multi-region storage bucket for maximum availability",
"Deploy in one zone of europe-west3 with very reliable VMs"],
a:0,
x:"Multiple zones within a German region satisfy both residency (data stays in Germany) and datacenter-failure resilience (zones are isolated facilities), and the resource-locations org policy prevents accidental drift. Belgium violates residency, EU multi-region buckets replicate outside Germany, and a single zone fails the risk requirement."},
{id:"d2-110",domain:2,topic:"Pricing",
q:"Your CTO asks for a cost estimate before approving a proposed architecture: 6 e2-standard-4 VMs running 24/7, a 500 GB Cloud SQL instance, and 2 TB of monthly internet egress. Nothing has been built yet. What is the fastest appropriate way to produce the estimate?",
o:["Model the components in the Google Cloud Pricing Calculator and share the estimate link",
"Build everything for one day and multiply the bill by 30",
"Enable billing export to BigQuery",
"Average the last three months of an unrelated project's invoices"],
a:0,
x:"The Pricing Calculator exists precisely for pre-build estimates and produces a shareable, itemized monthly figure. Building-and-extrapolating costs real money and misses monthly-cycle effects, billing export needs actual usage, and another project's spending has no relation to this design."},
{id:"d2-111",domain:2,topic:"Compute choice",
q:"Your data science team needs Jupyter-style experimentation VMs with GPUs during working hours only. Last month's bill shows the GPU VMs ran 24/7, wasting most of their cost. The scientists refuse to remember manual shutdowns. What should you implement?",
o:["Instance schedules that stop the VMs every evening and start them every workday morning",
"A sticky note on each monitor",
"Smaller GPUs so waste costs less",
"Moving the notebooks to Cloud Shell"],
a:0,
x:"Instance schedules (resource policies) automate stop/start on a calendar — removing the human factor entirely and cutting roughly two-thirds of always-on cost. Downgrading GPUs slows work while still wasting nights and weekends, and Cloud Shell has no GPUs and times out."},
{id:"d2-112",domain:2,topic:"Load balancing",
q:"Your SaaS product serves customers on every continent from backends in three regions. Product requirements: one IP address customers can allowlist, TLS termination managed by Google, URL-based routing (/api to the API backends, everything else to web), and automatic failover between regions. Which load balancer meets all of these?",
o:["Global external Application Load Balancer with a Google-managed certificate and a URL map",
"Regional external passthrough Network Load Balancer in each region with DNS round-robin",
"Internal Application Load Balancer",
"Cloud VPN"],
a:0,
x:"Only the global external ALB combines a single anycast IP, managed certs, L7 URL routing, and cross-region backend failover. Per-region NLBs need multiple IPs and DNS tricks with slow failover and no L7 routing; internal ALBs have no public endpoint; VPN is connectivity, not load balancing."},
{id:"d2-113",domain:2,topic:"Storage transfer",
q:"Your company is closing a data center in 45 days. It holds 900 TB of archival files that must land in Cloud Storage. The site's internet uplink is 1 Gbps and is 60% consumed by business traffic during the migration window. What should you do?",
o:["Order Transfer Appliances, load the archive locally, and ship them to Google, using the network only for the final delta",
"Run gsutil -m cp around the clock and hope",
"Upgrade the uplink to 2 Gbps and stream everything",
"Compress everything to fit the wire"],
a:0,
x:"900 TB over the free 400 Mbps of a shared 1 Gbps link needs roughly 8-9 months — physics rules out online transfer in 45 days. Transfer Appliance is designed for exactly this: bulk offline ingestion with a small online delta at cutover. Even a doubled uplink is months too slow, and archives rarely compress dramatically."},
{id:"d2-114",domain:2,topic:"Availability",
q:"After a zone outage took down their single-zone deployment for four hours, your client asks for the minimum change that prevents a repeat, without a major re-architecture. The app is a stateless web tier on a zonal managed instance group plus a Cloud SQL database. What should you recommend?",
o:["Convert the MIG to a regional (multi-zone) MIG and enable high availability on the Cloud SQL instance",
"Move everything to another provider",
"Add more VMs to the same zone",
"Take more frequent database backups"],
a:0,
x:"Regional MIGs spread identical instances across zones, and Cloud SQL HA adds a synchronously-replicated standby in another zone with automatic failover — both are configuration-level changes, not re-architecture. More VMs in the same zone die together; backups address data loss, not availability."},
{id:"d2-115",domain:2,topic:"GKE planning",
q:"Your platform team is choosing between GKE Standard and GKE Autopilot for a new microservices platform. The team is small, wants to be billed only for the resources pods actually request, and does not want to manage node upgrades, OS hardening, or bin-packing. Kubernetes API compatibility is required for their existing manifests. What should you choose?",
o:["GKE Autopilot",
"GKE Standard with a large node pool 'to be safe'",
"Compute Engine with kubeadm",
"Cloud Run, abandoning the Kubernetes manifests"],
a:0,
x:"Autopilot is full Kubernetes with Google operating the nodes and per-pod-resource billing — every stated requirement. Standard leaves node ops and idle-capacity cost with the team, kubeadm on VMs maximizes exactly the toil they refuse, and Cloud Run would force abandoning required K8s manifests."},
{id:"d2-116",domain:2,topic:"Networking",
q:"Security policy at your company prohibits public IP addresses on all VMs. A fleet of internal-only VMs must still download OS security patches from internet mirrors and call a partner's public REST API. Developers must also be able to SSH to these VMs for debugging. Which combination satisfies the policy? (Choose two.)",
o:["Cloud NAT for outbound internet access from the internal VMs",
"IAP TCP forwarding for SSH access without external IPs",
"A public IP on each VM, but with a strong firewall",
"An external load balancer pointing at each VM's SSH port"],
a:[0,1],
x:"Cloud NAT provides outbound-only internet (patches, partner API) and IAP tunnels administrative SSH through an identity-checked Google endpoint — the standard no-public-IP operating pattern. Public IPs violate the stated policy outright, and exposing SSH through a public load balancer is a security anti-pattern."},
{id:"d2-117",domain:2,topic:"Database choice",
q:"A logistics company wants its analysts to explore two years of shipment events — about 8 TB — with ad-hoc SQL, joining against reference tables, with no infrastructure to size or manage. Queries run a few dozen times per day. Which service and pricing model fit best?",
o:["BigQuery with on-demand (per-TB-scanned) pricing, using partitioned and clustered tables to keep scans small",
"BigQuery with maximum flat-rate slot reservations",
"Cloud SQL with the largest available machine",
"Dataproc with a persistent 20-node Hadoop cluster"],
a:0,
x:"Serverless ad-hoc analytics at moderate query volume is BigQuery on-demand's sweet spot, and partitioning/clustering keeps scanned bytes (the cost driver) down. Big slot reservations pay off only at sustained heavy usage; 8 TB ad-hoc scans overwhelm Cloud SQL; a persistent Hadoop cluster is idle cost plus ops burden."},
{id:"d2-118",domain:2,topic:"Compute choice",
q:"Your company runs a licensed third-party application whose vendor requires it to run on dedicated physical hardware not shared with other customers, for compliance reasons. The app itself is an ordinary Linux VM workload. What should you use?",
o:["Compute Engine sole-tenant nodes with node affinity for the application's VMs",
"Confidential VMs",
"Preemptible VMs with a dedicated label",
"GKE Autopilot"],
a:0,
x:"Sole-tenant nodes dedicate a whole physical server to your project — the compliance and BYOL isolation vehicle. Confidential VMs encrypt memory but still share hosts, labels don't change tenancy, and Autopilot abstracts hardware away entirely."},
{id:"d3-126",domain:3,topic:"MIGs",
q:"Your team runs a stateless API on a managed instance group behind an external Application Load Balancer. A new release must be rolled out during business hours with zero reduction in serving capacity, and it must stop automatically if new instances fail health checks. What should you do?",
o:["Start a rolling update on the MIG with maxSurge=3 and maxUnavailable=0, relying on the health check to gate progression",
"Delete all instances and let the MIG recreate them with the new template",
"SSH into each VM and update the binary in place",
"Resize the MIG to zero, update the template, and resize back up"],
a:0,
x:"maxUnavailable=0 forces the MIG to create surge instances first, so capacity never dips, and health-check failures halt the rollout. Mass deletion and resize-to-zero are outages by design, and hand-updating VMs diverges them from the template (the next autoheal reverts your work)."},
{id:"d3-127",domain:3,topic:"GKE",
q:"You are deploying a payment service to GKE. The security team requires that the service's pods authenticate to the Cloud KMS API as a dedicated identity with only the permissions the service needs — no exported keys, and no sharing the node's identity with other pods on the same node. What should you configure?",
o:["Workload Identity: bind the payment service's Kubernetes ServiceAccount to a dedicated Google service account granted only the needed KMS roles",
"Mount a service account JSON key into the pod via a Kubernetes Secret",
"Grant the node pool's default service account the KMS roles",
"Store a KMS admin key in an environment variable"],
a:0,
x:"Workload Identity gives each Kubernetes workload its own short-lived Google credentials — per-pod identity, least privilege, no key material. JSON keys are exactly what 'no exported keys' forbids, node-SA grants leak the permission to every pod on the node, and admin keys in env vars are a breach in waiting."},
{id:"d3-128",domain:3,topic:"Cloud Run",
q:"Your team ships a new revision of a revenue-critical Cloud Run service. Last quarter a bad release caused a 40-minute outage, and the postmortem action item was: 'no new revision may ever receive all production traffic immediately.' How should you deploy from now on?",
o:["Deploy with --no-traffic, route a small percentage (e.g., 5%) to the new revision with gcloud run services update-traffic, watch error rates, then shift gradually to 100%",
"Deploy normally, since Cloud Run revisions are always safe",
"Deploy to a different region and switch DNS at night",
"Reduce max instances so a bad release affects fewer users"],
a:0,
x:"Cloud Run's revision traffic splitting implements canary releases natively: tagged revisions can even be smoke-tested at a direct URL with 0% traffic first. Default deploys shift 100% immediately — precisely what the postmortem forbids. DNS games add latency and risk, and capping instances just degrades everyone."},
{id:"d3-129",domain:3,topic:"Firewall",
q:"Your three-tier application runs in a custom VPC: web VMs tagged web, application VMs tagged app, and a Cloud SQL database. Security requires that only web VMs can reach app VMs on port 8443, and nothing else can. Currently there are no custom firewall rules. What should you create?",
o:["An ingress allow rule on tcp:8443 with target tag app and source tag web (relying on the implied deny for everything else)",
"An ingress allow rule on tcp:8443 with source range 0.0.0.0/0",
"An egress deny-all rule on the web VMs",
"Nothing; VMs in the same VPC can always reach each other, which is fine"],
a:0,
x:"Source-tag → target-tag rules express exactly 'web may reach app on 8443', and the implied deny-all-ingress blocks everything else without extra rules. A 0.0.0.0/0 source opens the port to the world; the egress rule breaks the web tier's own traffic; and same-VPC reachability still requires ingress allows in a custom VPC (the requirement is to constrain, not permit all)."},
{id:"d3-130",domain:3,topic:"IaC",
q:"Your company creates a new project for every customer environment — about five per month — each needing the same baseline: a VPC, subnets, firewall rules, a GKE cluster, and IAM bindings. Engineers currently click this together in the console, and audits keep finding inconsistencies. Following Google-recommended practices, what should you do?",
o:["Codify the baseline as a Terraform module, store it in version control, and provision every new environment by applying the module through a CI pipeline",
"Write a wiki page with more detailed console screenshots",
"Record a video of a perfect console setup for training",
"Have one very careful engineer do all setups from now on"],
a:0,
x:"IaC turns environment creation into reviewed, versioned, repeatable code — identical every time, drift-detectable, auditable. Documentation and training reduce but never eliminate human variance, and a single careful human is a bottleneck and a bus-factor risk, not a control."},
{id:"d3-131",domain:3,topic:"Cloud SQL",
q:"You are provisioning the production database for a new line-of-business app: PostgreSQL, ~200 GB, must survive a zone failure with automatic failover, must be recoverable to any point in the last 7 days, and must not be reachable from the internet. Which configuration do you create?",
o:["Cloud SQL for PostgreSQL with the regional (HA) configuration, automated backups with point-in-time recovery enabled, and private IP only",
"Cloud SQL with a public IP restricted to the office's IP range",
"PostgreSQL on a single Compute Engine VM with nightly snapshots",
"Two independent Cloud SQL instances that the app writes to twice"],
a:0,
x:"This maps requirement-for-requirement: HA = zone failover, backups+PITR = 7-day recovery, private IP = no internet exposure. Office-IP whitelisting still exposes a public endpoint, a self-managed VM gives up managed HA/PITR, and double-writing to two instances is a consistency nightmare, not high availability."},
{id:"d3-132",domain:3,topic:"Pub/Sub",
q:"Your order-processing system must decouple the checkout web service from the fulfillment backend. Orders must not be lost even if fulfillment is down for a few hours, must be processed at least once, and fulfillment consumes them at its own pace from multiple workers. What should you build?",
o:["Publish orders to a Pub/Sub topic and have fulfillment workers consume from a pull subscription with acknowledgements",
"Have checkout call fulfillment synchronously with retries",
"Write orders to Memorystore and poll",
"Email each order to the fulfillment team's inbox"],
a:0,
x:"Durable buffering with at-least-once delivery, acknowledgement-based retention, and multiple pull consumers is Pub/Sub's exact contract — messages wait out downtime (default 7-day retention). Synchronous calls fail during outages, Redis is volatile so orders can vanish, and email is not a processing pipeline."},
{id:"d3-133",domain:3,topic:"Compute Engine",
q:"Your operations team must bring up 40 identical Linux VMs that install and configure nginx automatically at first boot, with no human SSH involved. The fleet will later need autoscaling and automatic replacement of failed instances. How should you build this from the start?",
o:["Create an instance template that includes a startup script (or a custom image with nginx baked in), then create a managed instance group from the template with autohealing enabled",
"Create 40 VMs in the console and SSH into each to install nginx",
"Create one perfect VM and clone its disk 39 times by hand",
"Use Cloud Shell to run apt-get in 40 tabs"],
a:0,
x:"Template + MIG is the assembly you'll need anyway for autoscaling/autohealing, and startup scripts (or a baked image) make bootstrap unattended. Console/SSH/tab approaches are toil now and leave you with unmanaged pets that can't autoscale or self-heal later."},
{id:"d3-134",domain:3,topic:"Serverless",
q:"Every night at 02:30, your team must generate a PDF report by running a containerized job that takes about 20 minutes and needs 4 GB of RAM. There is no HTTP traffic involved, and the team wants zero idle infrastructure. What is the Google-recommended assembly?",
o:["A Cloud Run job with 4 GB memory, triggered nightly by Cloud Scheduler",
"A Cloud Run service with min-instances=1 waiting all day for a request",
"A dedicated e2-standard-2 VM running cron",
"A GKE cluster with one CronJob"],
a:0,
x:"Cloud Run jobs run containers to completion without a listening port, and Cloud Scheduler provides the 02:30 trigger — zero cost between runs. An always-warm service or dedicated VM idles ~23.7 hours a day, and a whole cluster for one nightly job is the heaviest possible tool."},
{id:"d3-135",domain:3,topic:"Cloud Storage",
q:"Your legal team requires that objects in the contracts bucket, once written, cannot be modified or deleted by anyone — including project owners — for exactly five years. This must hold even if an administrator's account is compromised. What should you configure?",
o:["A bucket retention policy of five years, and lock the policy",
"Object versioning on the bucket",
"An IAM deny policy on delete permissions",
"A lifecycle rule deleting objects after five years"],
a:0,
x:"A LOCKED retention policy is irreversible WORM enforcement that binds even owners — the compliance-grade control. Unlocked policies and IAM can be changed by a compromised admin, versioning preserves copies but allows deletion of them, and a lifecycle delete rule is the opposite of retention."},
{id:"d3-136",domain:3,topic:"GKE",
q:"A junior engineer deployed a new image tag to your GKE production Deployment and error rates spiked immediately. The previous image tag is known-good. As the on-call engineer, what restores service fastest?",
o:["Run kubectl rollout undo deployment/<name> to revert to the previous ReplicaSet revision",
"Delete the whole Deployment and rewrite the manifest from memory",
"Restart all the nodes",
"Scale the Deployment to zero and investigate calmly"],
a:0,
x:"rollout undo flips back to the previous revision in seconds using state Kubernetes already keeps — the designed rollback. Deleting the Deployment or scaling to zero cause full outages, and node restarts don't change the bad image being served."},
{id:"d3-137",domain:3,topic:"Networking",
q:"Your company's static marketing site lives in a Cloud Storage bucket behind an external Application Load Balancer. Marketing bought the domain example-launch.com and wants it served over HTTPS with a certificate that renews itself, live before a product launch in two weeks. What should you do?",
o:["Add a Google-managed SSL certificate for example-launch.com to the load balancer's HTTPS proxy and point the domain's A record at the LB's IP in DNS",
"Buy a 10-year certificate and paste the private key into the bucket",
"Enable 'HTTPS' in the bucket settings",
"Use HTTP because certificates are complicated"],
a:0,
x:"Google-managed certificates provision and auto-renew once DNS points at the load balancer — the two-week timeline is comfortable. Buckets have no certificate settings and must sit behind an LB for custom-domain HTTPS; private keys don't belong in buckets; and shipping a launch site on plain HTTP fails modern browsers and basic security."},
{id:"d3-138",domain:3,topic:"BigQuery",
q:"An analyst is about to run exploratory SQL over a 30 TB events table in a project using on-demand BigQuery pricing. Finance has complained about surprise query bills before. Which TWO practices should you insist on before the analyst starts? (Choose two.)",
o:["Use dry runs (or the console estimator) to check bytes scanned before executing each query",
"Select only needed columns and filter on the table's partition column instead of SELECT * over the full table",
"Run every query twice to verify consistency",
"Export the table to CSV first and analyze it locally"],
a:[0,1],
x:"On-demand cost is bytes scanned: dry runs make cost visible before spending, and column selection + partition pruning shrink what's scanned. Running everything twice doubles cost for nothing, and exporting 30 TB to a laptop is impractical and creates an ungoverned data copy."},
{id:"d3-139",domain:3,topic:"Marketplace",
q:"Your team needs a production-grade GitLab installation on Google Cloud this week. Nobody on the team has deployed GitLab before, and the security team requires that it run inside your own project and VPC. What is the fastest reasonable path?",
o:["Deploy the vendor-maintained GitLab solution from Google Cloud Marketplace into your project, then harden its configuration",
"Read GitLab's source code and build a custom installer",
"Write your own Helm charts from scratch on a new GKE cluster",
"Sign up for a competitor's SaaS instead"],
a:0,
x:"Marketplace deployments land vendor-configured solutions in YOUR project/VPC with integrated billing — days of setup compressed to minutes, meeting the in-project requirement. Hand-building from source or scratch charts costs the week you don't have; external SaaS violates the requirement outright."},
{id:"d3-140",domain:3,topic:"Eventarc",
q:"Your imaging pipeline must generate a thumbnail whenever a photo is uploaded to the photos-raw bucket. The processing takes 2 seconds per image and volume is bursty — from zero to thousands of uploads per hour. The team wants event-driven processing with no servers to manage. What should you build?",
o:["A Cloud Run service (or function) triggered by the bucket's object-finalize events through Eventarc",
"A VM that lists the bucket every second and compares results",
"A daily batch job that processes yesterday's photos",
"A Dataproc cluster subscribed to the bucket"],
a:0,
x:"Storage finalize events → Eventarc → Cloud Run is the canonical serverless thumbnail pipeline: scales with bursts, costs nothing when idle. Polling wastes a VM and adds latency, daily batch fails the 'whenever uploaded' expectation, and Dataproc neither subscribes to buckets nor suits 2-second stateless tasks."}
);
