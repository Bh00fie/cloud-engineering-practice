// Domain 3: Deploying & implementing a cloud solution — part A (Compute Engine, disks, MIGs)
window.QUESTION_BANK = window.QUESTION_BANK || [];
window.QUESTION_BANK.push(
{id:"d3-001",domain:3,topic:"Compute Engine",
q:"Which command creates a VM named web-1 with machine type e2-medium in zone us-central1-a?",
o:["gcloud compute instances create web-1 --machine-type=e2-medium --zone=us-central1-a",
"gcloud compute create instance web-1 --type e2-medium",
"gcloud instances new web-1 e2-medium us-central1-a",
"gcloud vm create web-1"],
a:0,
x:"gcloud compute instances create is the canonical command; --machine-type and --zone set shape and placement (omitting --zone uses your configured default). The other forms are not valid gcloud syntax."},
{id:"d3-002",domain:3,topic:"Compute Engine",
q:"You need a VM to run a setup script automatically on first boot. What do you use?",
o:["A startup script via instance metadata (--metadata=startup-script=... or startup-script-url)",
"SSH in manually after boot",
"A cron job created after the fact",
"An image with the script name in its description"],
a:0,
x:"Compute Engine executes the startup-script metadata key at boot (every boot, in fact). It's the standard bootstrap mechanism; manual steps don't scale to MIGs, and descriptions are inert text."},
{id:"d3-003",domain:3,topic:"Compute Engine",
q:"How do you connect to a Linux VM's shell using gcloud?",
o:["gcloud compute ssh web-1 --zone=us-central1-a",
"gcloud compute rdp web-1",
"gcloud connect web-1",
"telnet web-1 22"],
a:0,
x:"gcloud compute ssh manages keys and connects (through IAP tunneling if configured for internal-only VMs). RDP is the Windows path (via a generated password and an RDP client); the other commands don't exist or are insecure."},
{id:"d3-004",domain:3,topic:"Compute Engine",
q:"To connect to a Windows Server VM you need credentials. Which command generates them?",
o:["gcloud compute reset-windows-password web-1 --zone=... --user=admin",
"gcloud compute ssh --windows",
"gcloud auth login on the VM",
"Passwords are emailed at creation"],
a:0,
x:"reset-windows-password creates/resets a local Windows account and returns the password for RDP use. SSH is not the default Windows path, and no password is emailed."},
{id:"d3-005",domain:3,topic:"Instance templates",
q:"What is an instance template?",
o:["An immutable definition of VM properties (machine type, image, disks, metadata) used to stamp out identical VMs, especially in MIGs",
"A running VM you clone live",
"A billing construct",
"A Kubernetes object"],
a:0,
x:"Templates capture VM configuration once; MIGs and gcloud can create instances from them. They're immutable — changes mean creating a new template version and rolling the group to it."},
{id:"d3-006",domain:3,topic:"MIGs",
q:"Which sequence correctly deploys an autoscaled, self-healing web tier?",
o:["Create instance template → create managed instance group from it → configure autoscaling and health-check-based autohealing → put it behind a load balancer",
"Create 10 VMs manually → label them 'group'",
"Create a load balancer → let it create VMs automatically",
"Create an unmanaged instance group with autoscaling"],
a:0,
x:"MIGs are built from templates and provide autoscaling, autohealing, and rolling updates; the LB fronts the group. Unmanaged groups can't autoscale or heal, labels don't make a group, and LBs never create VMs."},
{id:"d3-007",domain:3,topic:"MIGs",
q:"Which command resizes a managed instance group web-mig to 5 instances?",
o:["gcloud compute instance-groups managed resize web-mig --size=5 --zone=...",
"gcloud compute instances resize web-mig 5",
"gcloud mig scale web-mig 5",
"kubectl scale mig web-mig --replicas=5"],
a:0,
x:"instance-groups managed resize sets the target size (an autoscaler, if attached, may override it). kubectl manages Kubernetes, not MIGs, and the other forms are invalid."},
{id:"d3-008",domain:3,topic:"MIGs",
q:"Your MIG must replace VMs that stop responding on port 80 automatically. What do you configure?",
o:["Autohealing with an HTTP health check on the MIG",
"An uptime check in Monitoring",
"A firewall rule",
"Live migration"],
a:0,
x:"MIG autohealing recreates instances that fail the configured application health check. Monitoring uptime checks alert humans but don't recreate VMs; live migration handles host maintenance, not app failure."},
{id:"d3-009",domain:3,topic:"Disks & snapshots",
q:"You need a point-in-time backup of a persistent disk while the VM keeps running. What do you create?",
o:["A snapshot of the disk (optionally scheduled via snapshot schedules)",
"An image of the VM only possible when stopped",
"A local SSD copy",
"A bucket named after the disk"],
a:0,
x:"Snapshots capture disk state online (incremental after the first, stored regionally/multi-regionally) and restore to new disks anywhere. Custom images serve as boot templates; snapshots are the backup primitive."},
{id:"d3-010",domain:3,topic:"Disks & snapshots",
q:"Which statement about snapshots is TRUE?",
o:["After the first full snapshot, subsequent snapshots of the same disk store only changed blocks (incremental)",
"Every snapshot is a full copy, billed fully",
"Snapshots can only be restored in the same zone",
"Snapshots stop the VM automatically"],
a:0,
x:"Snapshots are incremental, keeping cost and time down, yet each is logically a full restore point; deleting one is safe because data is consolidated. They're global resources restorable to any zone/region, taken while VMs run."},
{id:"d3-011",domain:3,topic:"Disks & snapshots",
q:"A data disk on VM app-1 is nearly full. The least disruptive fix is:",
o:["Resize the disk larger (gcloud compute disks resize) then grow the filesystem in the OS — possible while attached and running",
"Delete the disk and buy a bigger one",
"Snapshot, delete VM, recreate everything",
"Compress the OS"],
a:0,
x:"Persistent disks grow online; after resizing you extend the partition/filesystem (e.g., resize2fs / xfs_growfs). Disks can never shrink, so start small and grow. No deletion or downtime required."},
{id:"d3-012",domain:3,topic:"Images",
q:"You customized a VM (installed agents, hardening) and want every future VM to start from that state. What do you create?",
o:["A custom image from the disk, referenced by instance templates",
"A snapshot only, restored manually each time",
"A README of manual steps",
"A label on the VM"],
a:0,
x:"Custom images are the reusable boot artifact for templates/MIGs; image families let you point at 'latest'. Snapshots work but images are the intended template primitive; manual steps and labels don't provision anything."},
{id:"d3-013",domain:3,topic:"Compute Engine",
q:"A VM needs to survive host hardware maintenance without downtime. Which Compute Engine capability does this by default for most VM types?",
o:["Live migration moves the running VM to another host transparently",
"Preemption",
"Autohealing",
"Nested virtualization"],
a:0,
x:"Live migration relocates VMs during infrastructure maintenance with no reboot (default onHostMaintenance=MIGRATE; some GPU/special VMs must terminate instead). Autohealing recreates failed app instances — different layer."},
{id:"d3-014",domain:3,topic:"Compute Engine",
q:"Which command lists your VMs with name, zone, status?",
o:["gcloud compute instances list",
"gcloud instances show",
"gcloud compute list vms",
"gcloud vm ls"],
a:0,
x:"gcloud compute instances list (with optional --filter/--format) is the inventory command. The others are invalid."},
{id:"d3-015",domain:3,topic:"Compute Engine",
q:"You must change the machine type of an existing VM. What's required?",
o:["Stop the VM, set the new machine type, start it again",
"Nothing — machine type changes live",
"Delete and recreate from scratch losing disks",
"A support ticket"],
a:0,
x:"Machine type changes require the instance to be TERMINATED (stopped); disks and IPs (if static or auto-kept) survive the stop/start. No recreation or support needed."},
{id:"d3-016",domain:3,topic:"Metadata",
q:"How can a running VM discover its own metadata (e.g., its service account token, custom values)?",
o:["Query the metadata server at http://metadata.google.internal (169.254.169.254) with the Metadata-Flavor: Google header",
"Call the public billing API",
"Read /etc/gcp.conf",
"It can't"],
a:0,
x:"The metadata server exposes instance and project metadata, including service account access tokens — how ADC works on VMs. There's no such config file, and the endpoint is link-local, not public."},
{id:"d3-017",domain:3,topic:"MIGs",
q:"You updated your instance template with a new app version. How do you deploy it to the MIG without dropping all capacity at once?",
o:["Start a rolling update: gcloud compute instance-groups managed rolling-action start-update with the new template and surge/unavailable settings",
"Delete the MIG and recreate it",
"SSH into each VM and update manually",
"Change the template in place"],
a:0,
x:"Rolling updates replace instances gradually (maxSurge/maxUnavailable control pace); canary updates can target a subset first. Templates are immutable so 'in place' is impossible; manual SSH edits drift from the template."},
{id:"d3-018",domain:3,topic:"Compute Engine",
q:"A batch VM should automatically be deleted after its work completes to save money. A clean pattern is:",
o:["The startup script runs the job and finishes by calling gcloud compute instances delete on itself (or use a Cloud Run job instead)",
"Set a calendar reminder",
"Spot VMs, which always delete themselves after work",
"Turn off billing"],
a:0,
x:"Self-deleting workers (or better, managed batch offerings like Cloud Run jobs / Batch) prevent idle burn. Spot preemption is involuntary and unrelated to job completion; reminders and billing toggles aren't automation."},
{id:"d3-019",domain:3,topic:"GKE",
q:"Which command creates a basic GKE cluster named apps with 3 nodes per zone in a region?",
o:["gcloud container clusters create apps --region=us-central1 --num-nodes=3",
"gcloud gke create apps 3",
"kubectl create cluster apps",
"gcloud compute clusters create apps"],
a:0,
x:"GKE clusters are managed via gcloud container clusters ...; --region makes a regional (multi-zone) cluster where --num-nodes applies per zone. kubectl talks to an existing cluster — it can't create one."},
{id:"d3-020",domain:3,topic:"GKE",
q:"After creating a cluster, kubectl says it has no configured context. What do you run?",
o:["gcloud container clusters get-credentials apps --region=us-central1",
"kubectl config new",
"gcloud auth login again",
"ssh into a node"],
a:0,
x:"get-credentials writes the cluster's endpoint and auth config into kubeconfig so kubectl can connect. Logging in again doesn't add cluster contexts, and nodes are not the management path."},
{id:"d3-021",domain:3,topic:"GKE",
q:"Which Kubernetes object keeps 4 replicas of a stateless pod running and manages rolling updates of its image?",
o:["A Deployment",
"A Service",
"A ConfigMap",
"A Namespace"],
a:0,
x:"Deployments manage ReplicaSets to maintain desired replica counts and orchestrate rolling updates/rollbacks. Services provide stable networking to pods; ConfigMaps hold configuration; Namespaces scope names."},
{id:"d3-022",domain:3,topic:"GKE",
q:"To expose a Deployment publicly on GKE with a single external IP at L4, the quickest object is:",
o:["A Service of type LoadBalancer",
"A Service of type ClusterIP",
"A DaemonSet",
"A CronJob"],
a:0,
x:"type: LoadBalancer provisions a Google Cloud passthrough load balancer with an external IP. ClusterIP is internal-only; for L7/HTTP routing you'd use Ingress or the Gateway API instead."},
{id:"d3-023",domain:3,topic:"GKE",
q:"Which command scales deployment web to 10 replicas?",
o:["kubectl scale deployment web --replicas=10",
"gcloud container scale web 10",
"kubectl resize pod web 10",
"gcloud compute instance-groups managed resize web --size=10"],
a:0,
x:"kubectl scale adjusts the Deployment's replica count (a HorizontalPodAutoscaler can manage it dynamically). gcloud doesn't scale K8s workloads, and resizing a MIG is the Compute Engine analog, not Kubernetes."},
{id:"d3-024",domain:3,topic:"GKE",
q:"Your pods need more nodes at peak and fewer at night, without manual intervention, in GKE Standard. What do you enable?",
o:["Cluster autoscaler on the node pool (min/max nodes)",
"A bigger control plane",
"kubectl cordon nightly",
"An external cron resizing the cluster hourly"],
a:0,
x:"The cluster autoscaler adds nodes when pods can't schedule and removes underutilized ones, within limits you set (pair with HPA for pod-level scaling). Cron resizing is crude; cordoning is for maintenance."},
{id:"d3-025",domain:3,topic:"GKE",
q:"Where should container images for a GKE deployment be stored (current best practice)?",
o:["Artifact Registry",
"A VM's local disk",
"Container Registry (gcr.io) — the recommended current product",
"Cloud Source Repositories"],
a:0,
x:"Artifact Registry is the current standard (Container Registry is deprecated/shut down for most uses); it stores Docker images plus language packages with regional repos and IAM. Source repos hold code, not images."},
{id:"d3-026",domain:3,topic:"GKE",
q:"kubectl apply -f deployment.yaml does what?",
o:["Creates or updates the resources declared in the manifest to match the file (declarative)",
"Only deletes resources",
"Builds a Docker image",
"Creates a new cluster"],
a:0,
x:"apply reconciles cluster state toward the manifest — the declarative workflow (vs. imperative kubectl create/run). Image builds happen elsewhere (Cloud Build, docker build), and clusters come from gcloud."},
{id:"d3-027",domain:3,topic:"Cloud Run",
q:"Deploy a container image to Cloud Run with public (unauthenticated) access in one command:",
o:["gcloud run deploy api --image=REGION-docker.pkg.dev/proj/repo/api:v1 --allow-unauthenticated --region=us-central1",
"gcloud app deploy --image",
"kubectl apply cloudrun.yaml --public",
"gcloud functions deploy api --container"],
a:0,
x:"gcloud run deploy creates/updates the service and --allow-unauthenticated grants run.invoker to allUsers. App Engine and functions are different products; Cloud Run's kubectl-style YAML exists but isn't deployed via kubectl."},
{id:"d3-028",domain:3,topic:"Cloud Run",
q:"A new Cloud Run revision misbehaves. What's the fastest safe rollback?",
o:["Route 100% traffic back to the previous revision (gcloud run services update-traffic api --to-revisions=PREV=100)",
"Delete the whole service and redeploy last week's code",
"Restart the container",
"Edit code live in the container"],
a:0,
x:"Cloud Run keeps prior revisions; traffic management flips back instantly with no rebuild. Deleting the service breaks the URL; containers are immutable and ephemeral — live edits aren't a thing."},
{id:"d3-029",domain:3,topic:"Cloud Run",
q:"You want to send 5% of traffic to a new Cloud Run revision before full rollout. Which feature?",
o:["Traffic splitting between revisions (gradual rollouts / canary)",
"A second project",
"A bigger CPU limit",
"Cloud CDN"],
a:0,
x:"Cloud Run natively splits traffic by percentage across revisions and supports tags for direct revision URLs — canary deployments without extra infrastructure."},
{id:"d3-030",domain:3,topic:"Cloud Run functions",
q:"Deploy a function triggered by messages on Pub/Sub topic orders:",
o:["gcloud functions deploy handle-order --runtime=... --trigger-topic=orders (2nd gen; Eventarc under the hood)",
"gcloud pubsub functions create",
"gcloud run deploy --trigger=pubsub",
"kubectl create function"],
a:0,
x:"--trigger-topic wires the function to Pub/Sub. (For Cloud Run services you'd create an Eventarc trigger separately; the functions command does it for you.) The other syntaxes are invalid."},
{id:"d3-031",domain:3,topic:"App Engine",
q:"Which command deploys an App Engine app from source with app.yaml present?",
o:["gcloud app deploy",
"gcloud app push",
"gcloud deploy app.yaml",
"appcfg upload"],
a:0,
x:"gcloud app deploy reads app.yaml and deploys a new version (optionally --no-promote to keep traffic on the old version). appcfg is long dead; the others don't exist."},
{id:"d3-032",domain:3,topic:"App Engine",
q:"You deployed an App Engine version with --no-promote. How do you shift 10% of traffic to it?",
o:["gcloud app services set-traffic default --splits=v2=0.1,v1=0.9",
"Redeploy without flags",
"Delete v1",
"It's automatic after an hour"],
a:0,
x:"set-traffic splits traffic between versions by weight (with optional --split-by for stickiness) — canary releases on App Engine. Deleting v1 would send 100% instantly, the opposite of a canary."},
{id:"d3-033",domain:3,topic:"Cloud Storage",
q:"Create a bucket for a new app in region europe-west1 with uniform access control:",
o:["gcloud storage buckets create gs://acme-app-assets --location=europe-west1 --uniform-bucket-level-access",
"gcloud buckets new acme-app-assets",
"gsutil create-bucket acme-app-assets",
"gcloud storage create gs://... --acl=fine-grained"],
a:0,
x:"gcloud storage buckets create (successor to gsutil mb) with uniform bucket-level access is the recommended setup — IAM-only permissions, no per-object ACLs. Bucket names are globally unique."},
{id:"d3-034",domain:3,topic:"Cloud Storage",
q:"Copy a local folder of 10,000 small files into a bucket efficiently:",
o:["gcloud storage cp -r ./assets gs://bucket/ (parallelized by default; gsutil needed -m)",
"One gsutil cp per file in a loop",
"FTP to the bucket",
"Email attachments"],
a:0,
x:"gcloud storage parallelizes transfers automatically (legacy gsutil required -m for parallelism). Buckets have no FTP endpoint; per-file serial copies are painfully slow."},
{id:"d3-035",domain:3,topic:"Cloud Storage",
q:"Objects older than 90 days should automatically move to Coldline, and be deleted after 3 years. What implements this?",
o:["A lifecycle policy on the bucket with SetStorageClass and Delete rules",
"A cron job on a VM running gsutil rewrite",
"Bucket versioning",
"Manual quarterly cleanup"],
a:0,
x:"Lifecycle rules run inside the service — conditions (age, class, versions) trigger actions (class transition, delete) with no infrastructure. A VM cron reimplements the feature poorly; versioning keeps MORE data, not less."},
{id:"d3-036",domain:3,topic:"Cloud SQL",
q:"Create a managed PostgreSQL instance:",
o:["gcloud sql instances create pgmain --database-version=POSTGRES_16 --region=us-central1 --tier=db-custom-2-8192",
"gcloud compute instances create --postgres",
"gcloud postgres new pgmain",
"kubectl create cloudsql"],
a:0,
x:"Cloud SQL instances are created via gcloud sql instances create with engine version, region, and machine tier. It's not a Compute Engine instance flag, and kubectl doesn't provision Cloud SQL."},
{id:"d3-037",domain:3,topic:"Cloud SQL",
q:"The recommended secure way for an app on Cloud Run to reach Cloud SQL is:",
o:["Cloud SQL connectors / built-in Cloud SQL connection (which handle encryption and IAM auth) rather than exposing a public IP with passwords",
"Public IP with 0.0.0.0/0 authorized",
"SSH tunnel through a bastion VM",
"Exporting the DB to a bucket nightly"],
a:0,
x:"Cloud Run integrates with the Cloud SQL Auth Proxy mechanism natively (--add-cloudsql-instances / connector libraries), giving encrypted, IAM-authorized connectivity. Whitelisting the world is a breach waiting to happen."},
{id:"d3-038",domain:3,topic:"BigQuery",
q:"Load a local CSV into a new BigQuery table with schema auto-detection:",
o:["bq load --autodetect --source_format=CSV dataset.table ./data.csv",
"gcloud bigquery import data.csv",
"bq insert dataset.table data.csv",
"gsutil cp data.csv bq://dataset"],
a:0,
x:"bq load ingests local files or GCS URIs; --autodetect infers schema. BigQuery has its own bq CLI (not gcloud bigquery for loads), bq insert is for JSON streaming rows, and bq:// isn't a real scheme."},
{id:"d3-039",domain:3,topic:"BigQuery",
q:"Before running a huge ad-hoc query on on-demand pricing, how can you check how much data it will scan?",
o:["Use a dry run (bq query --dry_run or the console's estimate) to see bytes processed",
"Run it and watch billing tomorrow",
"Count rows manually",
"Ask support"],
a:0,
x:"Dry runs validate the query and report bytes to be scanned without cost, letting you fix SELECT * mistakes before paying for them. The console shows the estimate as you type."},
{id:"d3-040",domain:3,topic:"Pub/Sub",
q:"Create a topic and a pull subscription for a new event pipeline:",
o:["gcloud pubsub topics create orders && gcloud pubsub subscriptions create orders-sub --topic=orders",
"gcloud pubsub create orders with-sub",
"gcloud messaging topics add orders",
"kubectl create topic orders"],
a:0,
x:"Topics receive messages; subscriptions deliver them (pull or push) — both created via gcloud pubsub. A topic without a subscription drops messages for subscribers that come later; create subscriptions before publishing matters."},
{id:"d3-041",domain:3,topic:"Pub/Sub",
q:"What's the difference between pull and push subscriptions?",
o:["Pull: subscribers request messages (scales well for backends); Push: Pub/Sub HTTP-POSTs each message to an endpoint (fits serverless webhooks)",
"Push is always faster and preferred",
"Pull deletes the topic after reading",
"Push requires a VM"],
a:0,
x:"Pull suits high-throughput consumers controlling their own pace; push delivers to an HTTPS endpoint like Cloud Run — no polling. Neither deletes topics; push specifically pairs well WITHOUT VMs."},
{id:"d3-042",domain:3,topic:"Marketplace",
q:"Deploying a WordPress solution from Cloud Marketplace results in:",
o:["Google Cloud resources (e.g., VM, disks) deployed into YOUR project with vendor-configured software, billed to your billing account",
"A SaaS hosted by Google outside your project",
"A container on Google's demo cluster",
"A license email only"],
a:0,
x:"Click-to-deploy Marketplace items provision real resources in your project — you manage and pay for them (plus any license fee). It's packaged IaC, not external SaaS (though Marketplace also lists SaaS separately)."}
);
