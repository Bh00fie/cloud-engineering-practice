// Domain 3: Deploying & implementing a cloud solution — part B (networking, IaC, data)
window.QUESTION_BANK = window.QUESTION_BANK || [];
window.QUESTION_BANK.push(
{id:"d3-043",domain:3,topic:"VPC",
q:"Your company needs a custom-mode VPC named corp with one subnet in us-east1 using range 10.10.0.0/20. Which commands?",
o:["gcloud compute networks create corp --subnet-mode=custom, then gcloud compute networks subnets create app --network=corp --region=us-east1 --range=10.10.0.0/20",
"gcloud compute networks create corp --auto-subnets=us-east1",
"gcloud vpc create corp 10.10.0.0/20",
"Subnets create themselves on first VM"],
a:0,
x:"Custom mode starts with no subnets; you add each with a region and CIDR. Auto mode would create subnets in every region from a fixed range — not what's asked. VMs require an existing subnet."},
{id:"d3-044",domain:3,topic:"Firewall",
q:"You must allow HTTPS from anywhere, but ONLY to the VMs tagged web in your VPC corp. Which command does this?",
o:["gcloud compute firewall-rules create allow-https --network=corp --allow=tcp:443 --source-ranges=0.0.0.0/0 --target-tags=web",
"gcloud compute firewall-rules create allow-https --allow=tcp:443 (applies to all VMs)",
"Open port 443 in Windows Firewall on each VM",
"Add tag 'https' to the VPC"],
a:0,
x:"Target tags scope the rule to tagged instances; without a target, the rule hits every VM in the network — a common over-exposure mistake. OS firewalls are additional, not the cloud control; tags attach to VMs, not VPCs."},
{id:"d3-045",domain:3,topic:"Firewall",
q:"Your new VM can't be reached on port 8080 even though the app is listening, and only default firewall rules exist. What's happening?",
o:["Ingress is denied by default; you must create an allow rule for tcp:8080 (and target the VM)",
"Egress is blocked by default",
"The VM needs a bigger machine type",
"Port 8080 is reserved by Google"],
a:0,
x:"VPCs have an implied deny-all ingress; the default network's pre-made rules cover SSH/RDP/ICMP/internal only, and custom VPCs have even less. Egress is allow-by-default; nothing about 8080 is special."},
{id:"d3-046",domain:3,topic:"Firewall",
q:"Your autoscaled fleet's VMs come and go constantly, and the firewall rules must keep applying to them. Best practice is to key rules to:",
o:["Service accounts (or network tags) on the instances, not individual IPs",
"Each VM's ephemeral IP",
"MAC addresses",
"Instance names"],
a:0,
x:"Rules targeting service accounts (strongest, IAM-governed) or tags follow instances automatically as they come and go. IPs change with autoscaling; names and MACs aren't valid firewall targets."},
{id:"d3-047",domain:3,topic:"Static IPs",
q:"Your load balancer's IP must never change because customers allowlist it. What do you do BEFORE creating the forwarding rule?",
o:["Reserve a static external IP address and reference it in the forwarding rule",
"Use the ephemeral IP and promise not to delete things",
"Buy the IP from a registrar",
"Use dynamic DNS"],
a:0,
x:"Reserved (static) addresses persist independently of resources; ephemeral IPs are lost on recreation. You can also promote an in-use ephemeral IP to static. Registrars sell domains, not Google Cloud IPs."},
{id:"d3-048",domain:3,topic:"Load balancing",
q:"You are debugging your global external Application Load Balancer and need to understand its component chain. Which resources compose it, in order?",
o:["Forwarding rule (IP:port) → target HTTPS proxy (+cert) → URL map → backend service(s) → instance groups/NEGs with health checks",
"One 'load balancer' object that contains everything",
"A DNS record and nothing else",
"VPN gateway → firewall → VM"],
a:0,
x:"The LB is assembled from these components; the URL map routes paths/hosts to backend services. Knowing this chain helps debug (cert on proxy, routing in URL map, health at backend service)."},
{id:"d3-049",domain:3,topic:"Cloud DNS",
q:"You must point www.example.com at your load balancer's IP 203.0.113.10 using Cloud DNS. What do you set up?",
o:["Create/verify a public zone for example.com, then add an A record for www → 203.0.113.10 (and delegate the domain's NS to Cloud DNS)",
"Add the IP to a firewall rule",
"Create a private zone",
"Rename the VM to www.example.com"],
a:0,
x:"Public serving requires a public zone whose name servers the registrar delegates to, plus the A/AAAA record. Private zones only resolve inside chosen VPCs; names and firewall rules don't publish DNS."},
{id:"d3-050",domain:3,topic:"Cloud VPN",
q:"You are setting up an HA VPN from your VPC to the company datacenter. Which components do you create on the Google side? (Choose two.)",
o:["An HA VPN gateway and tunnels to the peer",
"A Cloud Router for BGP route exchange",
"A Cloud CDN configuration",
"A GKE cluster"],
a:[0,1],
x:"HA VPN = VPN gateway + two tunnels + Cloud Router running BGP for dynamic routes (needed for the 99.99% SLA). CDN and GKE have nothing to do with site-to-site connectivity."},
{id:"d3-051",domain:3,topic:"IaC",
q:"Your team wants all infrastructure declared in versioned code with plan/preview before changes. The de-facto standard tool on Google Cloud today is:",
o:["Terraform with the Google provider (Infrastructure Manager as the managed runner)",
"Bash scripts of gcloud commands",
"Deployment Manager (recommended for all new work)",
"Editing the console faster"],
a:0,
x:"Terraform is the dominant IaC on Google Cloud (Google's Infrastructure Manager runs it as a service); it previews plans and stores state. Deployment Manager is legacy/deprecated-adjacent for new projects, and script piles have no state or plan."},
{id:"d3-052",domain:3,topic:"IaC",
q:"A teammate reviewing your CI pipeline asks what its terraform plan and terraform apply steps do respectively. What is the answer?",
o:["plan previews changes between config and real state; apply executes them",
"plan applies changes; apply schedules them",
"Both only validate syntax",
"plan destroys unmanaged resources"],
a:0,
x:"plan computes an execution diff (create/update/destroy) without touching anything; apply performs it (showing the plan again first by default). Nothing is destroyed unless the plan says so."},
{id:"d3-053",domain:3,topic:"Compute Engine",
q:"You need 20 identical VMs with the SAME configuration created NOW without a MIG. The efficient command is:",
o:["gcloud compute instances bulk create (or create with multiple names) using an instance template",
"20 separate console sessions",
"Cloning a running VM 19 times via snapshots manually",
"kubectl scale"],
a:0,
x:"Bulk instance creation stamps out many identical VMs from a template in one call. It's still usually better to use a MIG for lifecycle management — but the question excludes it. Manual cloning doesn't scale."},
{id:"d3-054",domain:3,topic:"GKE",
q:"You must add a second node pool with GPUs to your existing cluster, reserved for the ML team's pods only. What do you do?",
o:["gcloud container node-pools create gpu-pool --cluster=apps --accelerator=type=...,count=1 ... and schedule pods with node selectors/taints",
"Recreate the whole cluster",
"Attach GPUs to running nodes live",
"kubectl create nodepool"],
a:0,
x:"Node pools let one cluster host heterogeneous nodes; taints/tolerations or selectors steer only ML pods there. GPUs can't hot-attach to existing nodes, and kubectl doesn't manage GKE node pools."},
{id:"d3-055",domain:3,topic:"GKE",
q:"A developer on your team asks what they actually get from a Kubernetes Service of type ClusterIP. What does it give you?",
o:["A stable virtual IP inside the cluster that load-balances to the service's pods",
"An external IP",
"A DNS domain on the internet",
"A new node"],
a:0,
x:"ClusterIP is the default service type: stable internal endpoint + kube-dns name for pod-to-pod traffic. External exposure needs LoadBalancer/NodePort/Ingress."},
{id:"d3-056",domain:3,topic:"GKE",
q:"Pods in your production cluster keep crashing with OOMKilled status. Which manifest field addresses this?",
o:["resources.limits.memory (raise it) / right-size resources.requests.memory",
"replicas",
"terminationGracePeriodSeconds",
"imagePullPolicy"],
a:0,
x:"OOMKilled means the container exceeded its memory limit; adjust limits/requests (or fix the leak). Replica count and pull policy don't change per-container memory."},
{id:"d3-057",domain:3,topic:"Cloud Run",
q:"Your Cloud Run service must call other Google APIs as a dedicated identity with minimal permissions. What do you configure?",
o:["Deploy it with a user-managed service account (--service-account) granted only the needed roles",
"Use the default compute service account with Editor",
"Embed a JSON key in the container image",
"Use your personal user account"],
a:0,
x:"Per-service service accounts with least-privilege roles are the recommended identity pattern. Default-SA-with-Editor is the classic anti-pattern; keys in images leak; personal accounts don't belong in workloads."},
{id:"d3-058",domain:3,topic:"Cloud Run",
q:"Your internal-only Cloud Run service must never be reachable from the public internet. Which setting enforces this?",
o:["Ingress control set to 'internal' (and require authentication)",
"--allow-unauthenticated",
"A robots.txt",
"A strong container password"],
a:0,
x:"Cloud Run ingress settings restrict network reachability (internal / internal+LB / all), independent of IAM auth — use both layers. robots.txt is advisory to crawlers; containers don't have passwords."},
{id:"d3-059",domain:3,topic:"Data",
q:"Your data team has an existing PySpark pipeline and wants managed Apache Spark with minimal code changes. Which service?",
o:["Dataproc (clusters or serverless)",
"Dataflow",
"BigQuery only",
"Cloud Composer"],
a:0,
x:"Dataproc runs Spark/Hadoop natively — lift existing jobs as-is (Dataproc Serverless removes cluster management). Dataflow is Apache Beam (a rewrite), and Composer is workflow orchestration (managed Airflow), not an execution engine for Spark."},
{id:"d3-060",domain:3,topic:"Data",
q:"You are designing a new unified batch+streaming pipeline, written once and run fully managed with autoscaling. Google recommends:",
o:["Dataflow (Apache Beam)",
"Dataproc with cron",
"A big VM with Python scripts",
"Memorystore"],
a:0,
x:"Dataflow executes Beam pipelines with the same code for batch and streaming, autoscaling workers and handling shuffle. It's the default choice for new pipelines; the alternatives either add ops or don't do streaming semantics."},
{id:"d3-061",domain:3,topic:"Cloud SQL",
q:"Which TWO settings should production Cloud SQL instances almost always enable? ",
o:["Automated backups (with point-in-time recovery)",
"High availability (regional) configuration",
"Public IP open to 0.0.0.0/0",
"The smallest shared-core tier"],
a:[0,1],
x:"Backups+PITR protect data; HA protects uptime through zone failure. World-open public IP is a security hole, and shared-core tiers are for dev/test."},
{id:"d3-062",domain:3,topic:"Compute Engine",
q:"To cut batch costs, you create a VM with --provisioning-model=SPOT (or legacy --preemptible). This creates a VM that:",
o:["Costs much less but can be terminated by Google at any time (Spot; legacy preemptible also had a 24h max runtime)",
"Never restarts",
"Is free",
"Has guaranteed uptime SLA"],
a:0,
x:"Spot/preemptible VMs trade availability guarantees for deep discounts; workloads must checkpoint or be retryable. There's no SLA and they're not free."},
{id:"d3-063",domain:3,topic:"Networking",
q:"VMs in two different VPCs at your company must talk to each other over internal IPs. Options include VPC peering or...?",
o:["Connecting both to a Network Connectivity Center hub / using Shared VPC design instead",
"Public IPs with firewall allow",
"Copying data via bucket only",
"They can never communicate"],
a:0,
x:"Private inter-VPC connectivity options: VPC peering, NCC hub-and-spoke, or restructuring onto a Shared VPC. Public-IP hairpins violate the internal-only requirement."},
{id:"d3-064",domain:3,topic:"Cloud Storage",
q:"Marketing asks you to make just one object publicly readable in your uniform-access bucket. What is the reality?",
o:["Grant roles/storage.objectViewer to allUsers — but note uniform buckets do this at bucket level, so consider a signed URL for one object",
"gsutil acl ch -u AllUsers:R gs://bucket/obj works regardless of bucket mode",
"Rename the object public.txt",
"Email the object"],
a:0,
x:"Uniform bucket-level access disables per-object ACLs, so 'public' becomes a bucket-level IAM grant — which exposes the whole bucket. For a single object, a signed URL is usually the right tool. The ACL command fails on uniform buckets."},
{id:"d3-065",domain:3,topic:"GKE",
q:"You are on call and need to see the pods of namespace prod with their status and restart counts. Which kubectl command?",
o:["kubectl get pods -n prod",
"kubectl describe namespace prod",
"gcloud container pods list",
"kubectl logs prod"],
a:0,
x:"kubectl get pods lists pod status; describe gives detail on one resource; logs needs a pod name. gcloud has no pod commands — kubectl is the K8s interface."},
{id:"d3-066",domain:3,topic:"GKE",
q:"A pod in your cluster needs configuration values plus a database password injected at runtime. The Kubernetes-native pairing is:",
o:["ConfigMap for the config values; Secret (ideally synced from Secret Manager) for the password",
"Two ConfigMaps",
"Hardcode both in the image",
"A README"],
a:0,
x:"ConfigMaps carry non-sensitive config; Secrets carry sensitive values (base64, access-controlled, and best backed by an external manager). Baking credentials into images leaks them to anyone who can pull."},
{id:"d3-067",domain:3,topic:"Deployment",
q:"Your release process deploys the new version alongside the old and switches all traffic at once, with instant rollback by switching back. Which strategy is this?",
o:["Blue/green deployment",
"Rolling update",
"Canary release",
"Big-bang in place"],
a:0,
x:"Blue/green maintains two environments and flips traffic atomically. Rolling replaces instances gradually; canary sends a small slice first. Knowing the vocabulary is exam-relevant."},
{id:"d3-068",domain:3,topic:"Compute Engine",
q:"You must run a container image directly on a plain Compute Engine VM, without any Kubernetes involved. What is the simplest supported method?",
o:["Create the VM with --container-image (Container-Optimized OS runs it automatically)",
"Install a full GKE cluster on the VM",
"Convert the container to a VM image by hand",
"It's not possible"],
a:0,
x:"Deploying a container on a VM via Container-Optimized OS is built into instance creation. It's a lightweight middle ground; no cluster or manual conversion needed."},
{id:"d3-069",domain:3,topic:"Cloud SQL",
q:"You need to connect to your Cloud SQL instance from your laptop for admin work, without permanently allowlisting your IP. What do you use?",
o:["gcloud sql connect pgmain --user=postgres (which temporarily manages access) or run the Cloud SQL Auth Proxy",
"Open 0.0.0.0/0 on the instance",
"SSH to the instance's OS",
"Use the metadata server"],
a:0,
x:"gcloud sql connect whitelists your IP briefly and opens a client; the Auth Proxy provides IAM-authenticated, encrypted connectivity for regular use. You never get OS access to managed instances."},
{id:"d3-070",domain:3,topic:"BigQuery",
q:"An analyst on your team needs query access to one dataset without the ability to modify its tables. What do you grant?",
o:["Grant roles/bigquery.dataViewer on that dataset (plus jobUser on the project to run queries)",
"Project Editor",
"bigquery.admin",
"Share your own credentials"],
a:0,
x:"BigQuery IAM composes dataset-level data roles with the project-level ability to run jobs. dataViewer+jobUser is the least-privilege analyst bundle; Editor/admin over-grant wildly."},
{id:"d3-071",domain:3,topic:"Eventarc",
q:"You want your Cloud Run service invoked automatically whenever an object lands in your ingest bucket. What is the wiring?",
o:["An Eventarc trigger on the storage finalize event targeting the Cloud Run service",
"A bucket webhook setting",
"Polling the bucket from the service",
"A firewall rule"],
a:0,
x:"Eventarc routes CloudEvents (via Pub/Sub / audit logs) from sources like Cloud Storage to Cloud Run/GKE/functions. Buckets have no generic webhook config, and polling is the pattern events exist to replace."},
{id:"d3-072",domain:3,topic:"VPC",
q:"You are planning the subnets for your company's new VPC. Which range is valid for a subnet's primary IPv4 range?",
o:["Private RFC 1918 space like 172.16.0.0/16 (also some other allowed ranges), non-overlapping with other subnets in the VPC",
"8.8.8.0/24 because it's Google's",
"127.0.0.0/8",
"224.0.0.0/4 multicast"],
a:0,
x:"Subnets use private ranges (RFC 1918 and certain additional allowed blocks) that must not overlap within the network or with peers. Public, loopback, and multicast space are invalid/reserved."},
{id:"d3-073",domain:3,topic:"IaC",
q:"You are pitching IaC to a team at your company that manages everything by console clicking. Why is IaC preferred for production? (Choose two.)",
o:["Changes are reviewable/versioned and environments are reproducible",
"Drift is detectable and deployments are repeatable across projects",
"It makes resources run faster",
"It removes the need for IAM"],
a:[0,1],
x:"IaC buys review, history, reproducibility, and drift detection — governance benefits. It has zero effect on runtime performance and complements (never replaces) IAM."},
{id:"d3-074",domain:3,topic:"Compute Engine",
q:"The VM you are creating needs TWO extra data disks attached right at creation time. Which flag pattern applies?",
o:["Repeat --create-disk (or --disk) flags in gcloud compute instances create",
"Only one disk per VM is allowed",
"Attach disks only after boot, never at create",
"Use --disks=2"],
a:0,
x:"gcloud repeats the disk flags per disk (VMs support many attached disks, limits vary by machine size). Disks can also attach later, but creation-time is fine and common."},
{id:"d3-075",domain:3,topic:"GKE",
q:"A teammate reviewing your infrastructure asks why the GKE cluster is described as 'VPC-native'. What does that mean?",
o:["A cluster whose pods and services get IPs from subnet secondary (alias) ranges, making them first-class VPC citizens",
"A cluster with no networking",
"A cluster using only external IPs",
"An on-prem cluster"],
a:0,
x:"VPC-native (alias-IP) clusters draw pod/service IPs from the VPC, enabling native routing, firewall targeting, and private clusters. It's the default and recommended mode; routes-based clusters are legacy."},
{id:"d3-076",domain:3,topic:"Cloud Scheduler",
q:"Your team needs an HTTP endpoint invoked every night at 02:00, with retries and authentication handled. What do you use?",
o:["Cloud Scheduler job with cron '0 2 * * *' targeting the HTTP endpoint (with OIDC auth for Cloud Run)",
"A while-loop in Cloud Shell",
"An App Engine version per night",
"Manually"],
a:0,
x:"Cloud Scheduler is managed cron: HTTP, Pub/Sub, or App Engine targets, with retry policies and IAM-signed tokens for authenticated invocation. Cloud Shell sessions die; the rest isn't automation."},
{id:"d3-077",domain:3,topic:"Networking",
q:"You created a VM with no external IP for security reasons. How do you SSH to it without standing up a bastion host?",
o:["Use IAP TCP forwarding: gcloud compute ssh vm --tunnel-through-iap (with the IAP firewall range 35.235.240.0/20 allowed)",
"Give it a public IP temporarily every time",
"You cannot connect at all",
"Use FTP"],
a:0,
x:"Identity-Aware Proxy tunnels SSH/RDP over an authenticated Google endpoint — the modern bastion replacement. The firewall must allow IAP's source range to port 22; toggling public IPs is clumsy and riskier."},
{id:"d3-078",domain:3,topic:"Artifact Registry",
q:"Your CI machine has a locally built Docker image that must land in Artifact Registry. What are the steps to push it?",
o:["Configure auth (gcloud auth configure-docker REGION-docker.pkg.dev), tag the image with the repo path, docker push",
"gsutil cp image.tar gs://registry",
"kubectl push",
"Attach the image to an email to Google"],
a:0,
x:"Artifact Registry uses standard Docker protocol: configure the credential helper, tag REGION-docker.pkg.dev/PROJECT/REPO/IMAGE:TAG, push. Buckets and kubectl are not registries."},
{id:"d3-079",domain:3,topic:"App Engine",
q:"Your team wants to relocate its App Engine app to a different region next quarter. Which is TRUE about App Engine regions?",
o:["The app's region is chosen once at creation and cannot be changed later",
"You can move the app between regions monthly",
"Each version deploys to a different region automatically",
"Regions don't apply to App Engine"],
a:0,
x:"gcloud app create fixes the region permanently for that project's app — changing it means a new project. This immutability is a favorite exam fact."},
{id:"d3-080",domain:3,topic:"Cloud Storage",
q:"You must change the existing object gs://b/o to Coldline without re-uploading it from your machine. How?",
o:["gcloud storage objects update gs://b/o --storage-class=COLDLINE (or gsutil rewrite -s coldline)",
"Download and upload again as Coldline",
"Change the bucket's default class (affects existing objects retroactively)",
"Objects can't change class"],
a:0,
x:"Server-side rewrite changes an object's class in place. Bucket default class only applies to FUTURE uploads — a classic misconception; round-tripping through your laptop wastes bandwidth."},
{id:"d3-081",domain:3,topic:"GKE",
q:"A teammate runs kubectl expose deployment web --type=LoadBalancer --port=80 on your GKE cluster. What does it do?",
o:["Creates a Service; GKE provisions an external passthrough load balancer routing port 80 to the deployment's pods",
"Edits the deployment's image",
"Creates an Ingress with URL routing",
"Opens the firewall on all nodes for everything"],
a:0,
x:"expose generates a Service object; type LoadBalancer triggers cloud LB provisioning with an external IP. Ingress/Gateway are separate L7 constructs; only the needed forwarding is set up."},
{id:"d3-082",domain:3,topic:"Databases",
q:"You must migrate your company's on-prem MySQL database into Cloud SQL with minimal downtime. Which managed service?",
o:["Database Migration Service (continuous replication, then cutover)",
"mysqldump alone during a long outage",
"Storage Transfer Service",
"Dataproc"],
a:0,
x:"DMS replicates continuously from the source and lets you cut over when lag is near zero — minimal downtime, serverless, free for many homogeneous migrations. A dump/restore means downtime proportional to size; STS moves objects, not live databases."},
{id:"d3-083",domain:3,topic:"Compute Engine",
q:"You need to attach the existing disk data-1 to your running VM app-1 without any downtime. What do you do?",
o:["gcloud compute instances attach-disk app-1 --disk=data-1 --zone=... then mount it in the OS",
"Disks attach only at VM creation",
"gcloud compute disks join data-1 app-1",
"Copy the disk into the boot disk"],
a:0,
x:"attach-disk hot-attaches to a running instance (same zone); the OS then needs a mount (and a format on first use). No downtime required, and 'join' isn't a command."},
{id:"d3-084",domain:3,topic:"Serverless VPC",
q:"Your Cloud Run service must reach a Memorystore Redis instance at an internal VPC IP address. What is required?",
o:["Serverless VPC Access connector (or Direct VPC egress) so the service can reach the VPC's internal addresses",
"Nothing; serverless can always reach VPC IPs",
"A public IP on Redis",
"An interconnect"],
a:0,
x:"Serverless platforms sit outside your VPC; a VPC connector or Direct VPC egress bridges them to internal resources. Exposing Redis publicly is dangerous and often unsupported; interconnect is for on-prem."}
);
