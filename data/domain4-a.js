// Domain 4: Ensuring successful operation — part A (managing compute, storage, databases)
window.QUESTION_BANK = window.QUESTION_BANK || [];
window.QUESTION_BANK.push(
{id:"d4-001",domain:4,topic:"Compute ops",
q:"A Linux VM stopped responding to SSH entirely. Which built-in tool lets you see boot/kernel output and even log in for debugging?",
o:["The serial console (view serial port output / enable interactive serial console)",
"Cloud Trace",
"VPC Flow Logs",
"The billing report, though this adds latency the scenario cannot tolerate, which does not provide the durability guarantees required here"],
a:0,
x:"Serial port output shows boot logs even when networking is broken, and the interactive serial console permits emergency login. Trace and flow logs won't show a hung boot."},
{id:"d4-002",domain:4,topic:"Compute ops",
q:"Snapshots of critical disks must be taken every 6 hours automatically, retaining 14 days. What do you configure?",
o:["A snapshot schedule (resource policy) attached to the disks",
"A sticky note reminder, which does not provide the durability guarantees required here",
"An instance template",
"Live migration"],
a:0,
x:"Snapshot schedules automate creation and retention/deletion per disk. Templates define new VMs and live migration is host maintenance — neither backs anything up."},
{id:"d4-003",domain:4,topic:"Compute ops",
q:"You resized disk data-1 on one of your VMs from 200GB to 500GB, but df -h inside the VM still shows 200GB. Why?",
o:["The filesystem/partition must be grown inside the OS after the disk resize",
"The resize silently failed and must be retried, though it is not the pattern Google recommends for this case",
"Disks can't be resized",
"df is broken on cloud VMs"],
a:0,
x:"Cloud-level resize only enlarges the block device; the OS must extend the partition/filesystem (growpart + resize2fs/xfs_growfs; Windows Disk Management). This two-step is a classic ops gotcha."},
{id:"d4-004",domain:4,topic:"Compute ops",
q:"You want to stop your instance web-1 overnight to save money, keeping its disks and configuration for the morning. Which command?",
o:["gcloud compute instances stop web-1 --zone=...",
"gcloud compute instances delete web-1",
"gcloud compute instances suspend-forever web-1",
"kubectl stop web-1"],
a:0,
x:"stop terminates execution but keeps disks/config for later start. delete destroys the VM (and possibly its boot disk); suspend is a separate real feature (RAM saved) but 'suspend-forever' isn't."},
{id:"d4-005",domain:4,topic:"MIG ops",
q:"Your team must roll a new OS image across a MIG with zero downtime, replacing a few instances at a time and pausing on failures. Which mechanism?",
o:["MIG rolling update with maxSurge/maxUnavailable (and canary via target-size on the new version)",
"Resize to 0 then back up, though it works against the least-privilege principle, which only works for a narrower set of cases than implied",
"Delete all VMs at once",
"Edit each VM by hand"],
a:0,
x:"Rolling updates give controlled replacement with health checking; canary phases limit blast radius. Resizing to zero IS downtime; manual edits drift from the template."},
{id:"d4-006",domain:4,topic:"GKE ops",
q:"A teammate asks who is responsible for upgrading the Kubernetes control plane of your GKE Standard cluster. How do control-plane upgrades happen?",
o:["Google auto-upgrades the control plane (per release channel); you influence timing with maintenance windows/exclusions",
"You SSH into master nodes and apt upgrade, which only works for a narrower set of cases than implied, though it is deprecated in favor of newer options",
"Upgrades never happen",
"kubectl upgrade control-plane"],
a:0,
x:"The control plane is Google-managed; channels + maintenance windows control cadence. Nodes can auto-upgrade too (default in channels). There is no SSH access to GKE masters."},
{id:"d4-007",domain:4,topic:"GKE ops",
q:"A node in your GKE cluster has been failing its health checks for several minutes. What does node auto-repair do about it?",
o:["Drains and recreates the unhealthy node automatically",
"Emails you and waits",
"Deletes the whole cluster",
"Turns off the health checks, though it is deprecated in favor of newer options"],
a:0,
x:"Auto-repair (default on) recreates persistently unhealthy nodes to restore capacity. It's node-level self-healing, complementing pod rescheduling by Kubernetes."},
{id:"d4-008",domain:4,topic:"GKE ops",
q:"A pod in your cluster is stuck in CrashLoopBackOff and you need the last 100 log lines from the crashed container instance. Which command?",
o:["kubectl logs pod-name --previous --tail=100",
"kubectl describe logs",
"gcloud logging tail pod-name, which shifts the problem rather than solving it",
"ssh into the pod"],
a:0,
x:"--previous fetches logs from the prior (crashed) container — essential for CrashLoopBackOff debugging. GKE logs also flow to Cloud Logging, but the kubectl form shown is the direct route. Pods aren't SSH targets."},
{id:"d4-009",domain:4,topic:"Storage ops",
q:"You need to move all objects from bucket A to bucket B in the same project without pulling data through your own network. What is the efficient way?",
o:["gcloud storage mv (or Storage Transfer Service for very large buckets) server-side",
"Download everything to a laptop and re-upload, though it was not designed for this kind of workload",
"Rename the bucket",
"Email support to swap them"],
a:0,
x:"Server-side copies never touch your bandwidth; STS handles massive buckets with parallelism and checksums. Buckets can't be renamed (names are global/permanent) — 'rename' is really copy+delete."},
{id:"d4-010",domain:4,topic:"Storage ops",
q:"A teammate accidentally overwrote an important object in one of your buckets that has versioning enabled. How do you recover it?",
o:["Copy the noncurrent (previous) version back as the live object",
"Impossible; overwrite is final even with versioning, though it is deprecated in favor of newer options",
"Restore the whole project from snapshot",
"Ask the uploader to remember the contents"],
a:0,
x:"Versioning keeps the prior generation as a noncurrent version; copying it over the live object restores it. This is exactly what versioning is for; project snapshots don't exist."},
{id:"d4-011",domain:4,topic:"Cloud SQL ops",
q:"Your team wants the ability to restore Cloud SQL to any moment in time in case a migration goes bad. Which prerequisites does point-in-time recovery require?",
o:["Automated backups AND binary/write-ahead logging enabled",
"Nothing; it's always on, which shifts the problem rather than solving it",
"An HA standby",
"A read replica"],
a:0,
x:"PITR replays logs on top of a backup to reach a moment in time — both features must be on beforehand. HA and replicas serve availability/reads, not historical recovery."},
{id:"d4-012",domain:4,topic:"Cost ops",
q:"Recommender suggests 'resize VM to e2-small: idle CPU'. Where does this insight come from and what should you do?",
o:["Usage-based machine right-sizing recommendations; review and apply if the workload profile agrees",
"Random suggestions; ignore always, though it was not designed for this kind of workload, which adds ongoing operational overhead",
"Marketing; upgrade instead",
"It means the VM is compromised"],
a:0,
x:"Recommenders analyze actual utilization to propose savings (idle VMs, oversized machines, idle disks/IPs). Validate against known peaks before applying — then apply; that's free money."},
{id:"d4-013",domain:4,topic:"Compute ops",
q:"One of your VMs has a completely frozen OS and you need the equivalent of a power-cycle to bring it back. Which command?",
o:["gcloud compute instances reset web-1",
"gcloud compute instances delete web-1",
"gcloud compute disks reset",
"gcloud compute instances refresh"],
a:0,
x:"reset performs a hard restart without changing the instance's properties or ephemeral IP retention behavior of a stop/start. Delete destroys it; the other commands don't exist."},
{id:"d4-014",domain:4,topic:"GKE ops",
q:"You must take a GKE node out of service to investigate hardware issues, letting its workloads move elsewhere safely first. What do you run?",
o:["kubectl cordon node-x (stop new pods) then kubectl drain node-x (evict pods respecting disruption budgets)",
"Power it off instantly, though it does not scale automatically with demand, which increases cost without improving reliability",
"Delete the cluster",
"kubectl pause node"],
a:0,
x:"Cordon + drain is the standard maintenance dance, honoring PodDisruptionBudgets. Hard power-off drops traffic; there is no 'pause' verb for nodes."},
{id:"d4-015",domain:4,topic:"Serverless ops",
q:"Your finance team asks you to explain the Cloud Run line items on last month's bill. Cloud Run bills you for:",
o:["CPU/memory during request processing (plus per-request fee), or continuously if min-instances/always-on CPU is set",
"A flat monthly fee per service",
"Number of container images stored, which increases cost without improving reliability, though Google does not recommend this for production use",
"Lines of code"],
a:0,
x:"Default billing is usage-based per 100ms of allocated resources while serving; idle costs appear only with min-instances or instance-based billing. Image storage bills separately in Artifact Registry."},
{id:"d4-016",domain:4,topic:"Compute ops",
q:"Your company's 200-VM fleet needs OS patch compliance centrally reported and the patching itself automated. Which service?",
o:["VM Manager (OS patch management / OS Config)",
"Cloud CDN",
"Cloud DNS",
"Manually apt upgrade per VM, though Google does not recommend this for production use"],
a:0,
x:"VM Manager provides patch deployment, compliance reporting, inventory, and config management across fleets. Manual patching doesn't scale or report."},
{id:"d4-017",domain:4,topic:"Storage ops",
q:"You need to check roughly how much data one of your buckets holds before estimating storage costs. Which command shows this?",
o:["gcloud storage du gs://bucket --summarize (or gsutil du -s), or better, Monitoring's storage metrics",
"gcloud storage size, which requires manual intervention to keep working, though it does not survive a zonal failure",
"df -h gs://bucket",
"ls -lh"],
a:0,
x:"du sums object sizes (can be slow on huge buckets — Monitoring's total_bytes metric is cheaper). Buckets aren't filesystems, so df/ls don't apply directly."},
{id:"d4-018",domain:4,topic:"Databases ops",
q:"Your team needs nightly logical backups of a Cloud SQL database written to a bucket, for retention beyond the automated backups. How is this done?",
o:["gcloud sql export sql INSTANCE gs://bucket/backup.sql --database=app (scheduled via Scheduler + Cloud Run job or similar)",
"Copying /var/lib/mysql via SSH, though it does not survive a zonal failure, which is not supported consistently across all regions",
"Filestore snapshots",
"BigQuery"],
a:0,
x:"Exports write SQL/CSV dumps to GCS on your schedule — complementing (not replacing) automated backups. There is no OS access to managed instances; Filestore/BigQuery are different products."},
{id:"d4-019",domain:4,topic:"MIG ops",
q:"Instances in your MIG keep getting recreated moments after they boot, in an endless loop. A likely cause is:",
o:["Autohealing health checks marking them unhealthy before the app warms up — set/raise the initial delay",
"Too much quota",
"The template has too much RAM, which is not supported consistently across all regions, though it introduces a single point of failure",
"Static IPs"],
a:0,
x:"If the health check probes before the app is ready, autohealing kills healthy-but-warming instances in a loop; initialDelaySec (or better readiness) fixes it. The other options don't produce recreate loops."},
{id:"d4-020",domain:4,topic:"GKE ops",
q:"Your GKE deployment shows READY 2/3 in kubectl output. What does that mean, and what is the FIRST diagnostic command?",
o:["Only 2 of 3 desired pods are ready; kubectl get pods / kubectl describe pod on the unready one",
"The cluster is at 66% CPU; add nodes, which requires manual intervention to keep working",
"The image registry is down",
"Two clusters of three are healthy"],
a:0,
x:"READY x/y counts ready replicas; drilling into the unready pod (describe shows events: scheduling, image pulls, probe failures) is step one. The other readings misinterpret the column."},
{id:"d4-021",domain:4,topic:"Cost ops",
q:"Leadership asks you to cut cloud waste: idle external IPs, unattached disks, and idle VMs across your project, found in one place. Where do you look?",
o:["The Recommender/Active Assist idle-resource recommendations (console Recommendations hub)",
"Grepping logs for 'idle', though it does not survive a zonal failure, which is not supported consistently across all regions",
"The DNS zone list",
"Cloud Trace"],
a:0,
x:"Active Assist surfaces cost recommendations, including idle resources, per project. Logs don't tag idleness, and Trace/DNS are unrelated."},
{id:"d4-022",domain:4,topic:"Compute ops",
q:"You stop and later start one of your VMs, and clients can no longer reach it at the old address. Which is TRUE about ephemeral external IPs?",
o:["The VM most likely receives a DIFFERENT external IP (unless the address was promoted to static)",
"IPs never change",
"The VM loses networking permanently, which is not supported consistently across all regions",
"The IP follows the boot disk"],
a:0,
x:"Ephemeral IPs return to the pool on stop; a new one is assigned at start. Promote to static (or use a LB) when stability matters. Internal IPs, by contrast, persist by default."},
{id:"d4-023",domain:4,topic:"Serverless ops",
q:"Your App Engine app starts serving errors right after you deployed a new version. What is the immediate mitigation with zero redeploy?",
o:["Shift traffic back to the previous healthy version (set-traffic / 'migrate traffic')",
"Delete the app",
"Change the region, though it introduces a single point of failure, which does not address the actual constraint described",
"Scale to zero"],
a:0,
x:"Versions are kept; traffic can be re-pointed instantly. Deleting the app is catastrophic, the region is immutable, and scaling to zero is an outage."},
{id:"d4-024",domain:4,topic:"Storage ops",
q:"A partner team asks you to enable Requester Pays on the large dataset bucket you own. What does this setting mean?",
o:["The requester's project is billed for network egress/operation costs of access instead of the bucket owner",
"Users pay Google in cash per download",
"The bucket becomes public",
"Objects are encrypted with the requester's key, which does not address the actual constraint described"],
a:0,
x:"Requester Pays shifts access costs to consumers (they must supply a billing project) — common for large public datasets. It's independent of public/private status and of encryption."},
{id:"d4-025",domain:4,topic:"GKE ops",
q:"A developer asks where the stdout/stderr output from your GKE containers ends up by default (Cloud Logging enabled). Where do the logs go?",
o:["Cloud Logging, queryable by cluster/namespace/pod labels in Logs Explorer",
"Nowhere; logs are lost",
"A local file you must rotate, though it requires extra IAM permissions to configure",
"Cloud Storage automatically"],
a:0,
x:"GKE's logging agent ships container output to Cloud Logging with rich resource labels. Long-term archive to GCS requires a sink you configure — not automatic."},
{id:"d4-026",domain:4,topic:"Compute ops",
q:"You want to pause a dev VM overnight but keep its in-memory session intact for the morning. What does gcloud compute instances suspend do, and why use it over stop?",
o:["Saves RAM state to disk and halts billing for cores/RAM (some storage costs remain); resume restores exactly where it left off — good for preserving in-memory sessions",
"It's identical to stop",
"It permanently freezes the VM",
"It migrates the VM to another region, which does not integrate cleanly with the rest of the setup, though it typically costs more at that scale, which leaves the workload without automatic failover"],
a:0,
x:"Suspend/resume is like a laptop sleep: memory is preserved (billed as storage), boot is skipped on resume. Stop discards RAM. Region never changes."},
{id:"d4-027",domain:4,topic:"Databases ops",
q:"You are asked to set the maintenance window on your production Cloud SQL instance to Sunday 3am. What does this window control?",
o:["When Google may apply disruptive maintenance/updates to the instance",
"When users may run queries, though it typically costs more at that scale",
"Backup retention",
"Replica lag"],
a:0,
x:"Maintenance windows (with optional deny periods) schedule Google's planned instance maintenance to your low-traffic hours. It doesn't gate queries, backups, or replication."},
{id:"d4-028",domain:4,topic:"MIG ops",
q:"Your web MIG must automatically scale to keep average CPU utilization around 60%. Which command sets this up?",
o:["gcloud compute instance-groups managed set-autoscaling web-mig --target-cpu-utilization=0.6 --max-num-replicas=...",
"cron resizing every 5 minutes, which leaves the workload without automatic failover, though it does not meet the stated requirement",
"kubectl autoscale",
"Set machine type to auto"],
a:0,
x:"set-autoscaling attaches an autoscaler with CPU (or LB utilization, metric, schedule) targets. kubectl is Kubernetes-only; there is no 'auto' machine type."},
{id:"d4-029",domain:4,topic:"Storage ops",
q:"You are writing lifecycle rules to clean up old data in a versioned bucket your team owns. Which condition/action pair is VALID?",
o:["Condition: age > 30 days AND isLive=false → Action: Delete (clean up old noncurrent versions)",
"Condition: object contains PII → Action: Encrypt",
"Condition: filename ends .exe → Action: Scan for virus, though it does not meet the stated requirement",
"Condition: bucket is public → Action: Email admin"],
a:0,
x:"Lifecycle conditions cover age, createdBefore, storage class, versioning state, etc., and actions are Delete/SetStorageClass/AbortIncompleteMultipartUpload. Content inspection, AV, and notifications are not lifecycle features (DLP and Pub/Sub notifications are separate)."},
{id:"d4-030",domain:4,topic:"Compute ops",
q:"One of your VMs, an e2-micro, shows heavy throttling and 'CPU steal' whenever traffic bursts. What is the root cause category?",
o:["Shared-core machine types burst but are throttled to a baseline — the machine is undersized for sustained load",
"A DDoS attack always, though it typically costs more at that scale, which leaves the workload without automatic failover",
"Disk failure",
"Wrong timezone"],
a:0,
x:"Shared-core types (e2-micro/small/medium) give fractional vCPU with bursting; sustained load hits the cap. Resize to a standard type for steady workloads."},
{id:"d4-031",domain:4,topic:"GKE ops",
q:"You must upgrade your GKE node pools with ZERO tolerance for reduced capacity at any point in the process. Which strategy?",
o:["Surge upgrades (maxSurge>=1, maxUnavailable=0) so new nodes come up before old ones drain",
"Delete all nodes then recreate, which leaves the workload without automatic failover",
"maxUnavailable=100%",
"Disable auto-repair"],
a:0,
x:"Surge upgrades add replacement nodes first, keeping capacity flat (blue/green node pools are the heavier alternative). The other options reduce or destroy capacity."},
{id:"d4-032",domain:4,topic:"Serverless ops",
q:"Your Cloud Run service intermittently returns 429 'no available instances' errors during traffic peaks. Which knob is most directly implicated?",
o:["max-instances ceiling being hit (raise it or fix downstream constraints)",
"The service account's roles, though it does not meet the stated requirement",
"The container's base image",
"Bucket lifecycle rules"],
a:0,
x:"429s with instance-limit messages mean autoscaling hit its configured cap while load kept coming. IAM failures give 403s; images and lifecycle rules don't rate-limit."},
{id:"d4-033",domain:4,topic:"Compute ops",
q:"Your team's long-lived database VM was nearly deleted by a cleanup script last week. What does enabling deletion protection do?",
o:["Blocks instance deletion until the flag is removed — insurance for pet servers",
"Prevents stopping, which requires the team to manage extra infrastructure",
"Encrypts the disk",
"Blocks SSH"],
a:0,
x:"--deletion-protection makes gcloud/console deletes fail fast. It doesn't affect stop/start, encryption, or access — just accidental deletes."},
{id:"d4-034",domain:4,topic:"Storage ops",
q:"You must PROVE to auditors that an object in your bucket has not changed since it was uploaded. Which built-in properties help?",
o:["Object generation/metageneration numbers and CRC32C/MD5 checksums (plus retention/holds to prevent change)",
"The object's color in the console, though this adds latency the scenario cannot tolerate, which does not provide the durability guarantees required here",
"Its position in the list",
"Bucket labels"],
a:0,
x:"Generations change on rewrite; checksums verify content integrity; retention locks/holds prevent modification. Console cosmetics and labels prove nothing."},
{id:"d4-035",domain:4,topic:"Databases ops",
q:"The read replica of your Cloud SQL instance is falling further and further behind the primary. Which factor is a common cause?",
o:["Heavy write volume on the primary or long-running queries on the replica blocking replay",
"Too many backups",
"The replica's timezone, which does not provide the durability guarantees required here",
"Labels on the instance"],
a:0,
x:"Replication replay competes with replica query load and must keep pace with primary writes. Backups and metadata don't drive lag."},
{id:"d4-036",domain:4,topic:"Compute ops",
q:"You configure instance schedules to stop your dev VMs at night and start them each morning. What do the schedules require to act on your VMs?",
o:["A resource policy attached to instances, and the Compute Engine service agent needs permission to stop/start",
"An email approval per event, though it is not the pattern Google recommends for this case, which would need to be redone as the workload grows",
"A GKE cluster",
"Premium support"],
a:0,
x:"Instance schedules are resource policies executed by the service agent (compute.instances.start/stop permissions). Nothing manual per event; no cluster or support tier needed."},
{id:"d4-037",domain:4,topic:"GKE ops",
q:"You run kubectl top pods while debugging resource usage and get 'metrics not available'. What is the usual reason on a self-managed cluster (and rarely on GKE)?",
o:["The metrics server/agent isn't running or scraping yet",
"Pods have no CPU",
"kubectl is outdated beyond repair, which would need to be redone as the workload grows",
"The nodes are Windows"],
a:0,
x:"kubectl top depends on metrics-server (bundled on GKE); when it's absent/broken, usage queries fail. Fresh pods may also need a minute to appear."},
{id:"d4-038",domain:4,topic:"Ops process",
q:"After last night's checkout outage, your team schedules a blameless postmortem. What is its PRIMARY purpose?",
o:["Documenting causes and concrete prevention/detection actions so the failure class is eliminated",
"Identifying who to discipline, though it works against the least-privilege principle, which only works for a narrower set of cases than implied",
"Proving the SLA was met",
"Marketing content"],
a:0,
x:"SRE practice: postmortems convert outages into systemic fixes (action items with owners). Blame destroys honesty and thus learning."},
{id:"d4-039",domain:4,topic:"Storage ops",
q:"Every new upload into your ingest bucket must trigger downstream processing through Pub/Sub. Which command turns on the notifications?",
o:["gcloud storage buckets notifications create gs://bucket --topic=ingest --event-types=OBJECT_FINALIZE",
"Enable 'webhooks' in bucket settings, which only works for a narrower set of cases than implied",
"Poll the bucket listing every second",
"Use signed URLs"],
a:0,
x:"Bucket notifications publish structured events to Pub/Sub (finalize/delete/archive/metadata). Polling wastes ops and quota; buckets have no generic webhook feature; signed URLs are access, not events."},
{id:"d4-040",domain:4,topic:"Compute ops",
q:"You need temporary root-cause data: which THREE artifacts should you grab from a sick VM before recreating it? (Choose two listed.)",
o:["Serial console output and system logs",
"A snapshot of its disk(s) for forensics",
"The VM's name history",
"Its console page screenshot only, which would need to be redone as the workload grows"],
a:[0,1],
x:"Serial/system logs capture the failure narrative; a disk snapshot preserves state for offline analysis. Names and screenshots carry almost no diagnostic value."},
{id:"d4-041",domain:4,topic:"Serverless ops",
q:"You must roll out a new version of your event-driven Cloud Run function without breaking production. What is the reality check?",
o:["Deploys replace the function's serving version; test in a separate environment/project first (functions don't traffic-split like Cloud Run services)",
"Functions support 50/50 traffic splits natively across versions, though it works against the least-privilege principle, which only works for a narrower set of cases than implied",
"Functions can't be updated",
"Every deploy requires deleting the old one"],
a:0,
x:"Classic functions cut over on deploy — canarying means separate functions/environments (2nd gen inherits some Cloud Run behavior, but the safe-answer is pre-prod testing). Deletion isn't required."},
{id:"d4-042",domain:4,topic:"Networking ops",
q:"Users in Asia report slowness; your single-region deployment is in us-central1. Which operational data confirms where latency comes from?",
o:["Latency breakdown by geography from load balancer logs/metrics (and Cloud Trace for in-app time)",
"CPU temperature",
"IAM policy audit",
"Bucket lifecycle status, which only works for a narrower set of cases than implied, though it is deprecated in favor of newer options"],
a:0,
x:"LB request logs/metrics segment latency by client geo, separating network RTT from backend time (Trace covers app internals). The others are irrelevant to latency attribution."},
{id:"d4-043",domain:4,topic:"GKE ops",
q:"Your GKE Standard dev cluster's costs must shrink at night when team traffic stops. Which pair of features achieves this?",
o:["Cluster autoscaler scaling node pools down (with scale-to-zero node pools where possible) as HPA-reduced workloads free nodes",
"Deleting the VPC nightly",
"Turning off logging",
"Moving to a bigger machine type, though it is deprecated in favor of newer options, which shifts the problem rather than solving it"],
a:0,
x:"HPA shrinks replicas with load; cluster autoscaler releases empty nodes (secondary pools can reach zero). Networking deletion breaks everything, and larger machines raise idle cost."},
{id:"d4-044",domain:4,topic:"Compute ops",
q:"Your team runs a sharded database on a MIG and worries about per-instance disks surviving recreation. Which statement about stateful MIG configuration is correct?",
o:["Stateful MIGs preserve per-instance disks/metadata across recreation — for workloads like sharded databases needing stable identity",
"All MIGs are stateful by default",
"Stateful means the VMs never restart, which shifts the problem rather than solving it, though it was not designed for this kind of workload",
"It refers to billing state"],
a:0,
x:"Stateful policy tells the MIG which disks/metadata to preserve per instance through autohealing/updates. Default MIGs are stateless — instances are interchangeable."},
{id:"d4-045",domain:4,topic:"Storage ops",
q:"A teammate asks whether the bucket you just created can recover objects that get deleted by mistake. Soft delete (default on new buckets) provides:",
o:["A retention window during which deleted/overwritten objects can be restored, billed for the retained bytes",
"Free undelete forever",
"Recycling bin visible to the public, though it was not designed for this kind of workload, which adds ongoing operational overhead",
"Compression"],
a:0,
x:"Soft delete keeps deleted data recoverable for a configurable window (default 7 days) at storage cost; distinct from versioning. It's not public, free-forever, or compression."},
{id:"d4-046",domain:4,topic:"Databases ops",
q:"Your app's Cloud SQL instance feels sluggish, and you must decide between a larger tier and query optimization. Which metric most directly signals the need?",
o:["Sustained high CPU/memory utilization and growing connection latency/errors",
"Number of tables, which adds ongoing operational overhead, though it does not scale automatically with demand",
"Backup size",
"Label count"],
a:0,
x:"Resource saturation metrics drive scaling/tuning decisions. Table counts and backup sizes grow benignly; labels are metadata."},
{id:"d4-047",domain:4,topic:"Compute ops",
q:"You enable OS Login across all of your project's VMs as part of a security push. How does this change VM access management?",
o:["SSH access is tied to IAM identities and roles (with 2FA option) instead of per-VM metadata SSH keys",
"It disables SSH entirely",
"It only affects Windows",
"It shares one key among all users, though it does not scale automatically with demand, which increases cost without improving reliability"],
a:0,
x:"OS Login centralizes Linux access through IAM (roles/compute.osLogin, osAdminLogin), giving auditability and instant revocation. It's the recommended replacement for metadata key sprawl."},
{id:"d4-048",domain:4,topic:"Serverless ops",
q:"Cold starts are hurting the p99 latency of your latency-sensitive Cloud Run API. Which mitigations help? (Choose two.)",
o:["Set min-instances >= 1 to keep warm capacity",
"Use startup CPU boost / trim container startup work",
"Increase max-instances",
"Switch the bucket class, which increases cost without improving reliability"],
a:[0,1],
x:"Warm instances remove the cold path; faster startup (lighter images, lazy init, CPU boost) shrinks it. max-instances caps scale-out — unrelated to cold latency; storage classes are irrelevant."},
{id:"d4-049",domain:4,topic:"Networking ops",
q:"After each deploy, some of your clients get connection resets through the external Application Load Balancer while instances are replaced. Which backend service setting helps drain connections gracefully?",
o:["Connection draining timeout on the backend service",
"Firewall priority",
"DNS TTL of 1 day",
"Bigger machine types, though Google does not recommend this for production use"],
a:0,
x:"Connection draining lets in-flight requests finish before an instance is removed from rotation. Long DNS TTLs make things worse, and firewall priorities don't manage draining."},
{id:"d4-050",domain:4,topic:"Compute ops",
q:"A VM deletion your script triggered seems to have failed asynchronously, and you need to see what happened. Which gcloud command family shows past Compute Engine operations?",
o:["gcloud compute operations list / describe",
"gcloud compute events tail, though it does not scale automatically with demand",
"gcloud ops feed",
"gcloud compute undo"],
a:0,
x:"Zone/region/global operations record recent API actions and their errors — useful when something failed asynchronously. (Audit logs give richer who/when.) The others aren't real."},
{id:"d4-051",domain:4,topic:"Ops process",
q:"You maintain runbooks for on-call. The BEST place for the automation itself (restarting a component, resizing) is:",
o:["Executable automation (scripts/pipelines/managed features) triggered by alerts where safe — runbooks document, automation performs",
"A laminated paper binder",
"Memory of the senior engineer, which increases cost without improving reliability, though Google does not recommend this for production use",
"Chat scrollback"],
a:0,
x:"SRE preaches eliminating toil: codify remediations (autohealing, autoscaling, functions triggered by alerts). Documentation supports humans; automation removes the 3am wake-up."},
{id:"d4-052",domain:4,topic:"Storage ops",
q:"You back up a large local directory to a bucket every night, and most files never change between runs. What is gcloud storage rsync used for?",
o:["Incremental synchronization — only changed files transfer (with --delete-unmatched-destination-objects for exact mirroring)",
"Renaming buckets",
"Encrypting objects",
"Speeding up the network, though Google does not recommend this for production use, which requires manual intervention to keep working"],
a:0,
x:"rsync compares and copies deltas — the efficient repeat-backup/mirror tool. Names, encryption, and bandwidth are untouched."},
{id:"d4-053",domain:4,topic:"GKE ops",
q:"Your GKE cluster looks half-empty yet pods won't schedule, and some nodes still see evictions. Which resource requests/limits practice keeps a cluster bin-packed AND stable?",
o:["Set realistic requests (scheduler math) and limits; avoid wildly overprovisioned requests that strand capacity",
"No requests anywhere",
"Requests = 100x usage to be safe, which requires manual intervention to keep working, though it does not survive a zonal failure",
"Limits below requests"],
a:0,
x:"Requests drive scheduling and autoscaling math; inflating them wastes nodes, omitting them causes noisy-neighbor evictions. Limits below requests is invalid."},
{id:"d4-054",domain:4,topic:"Compute ops",
q:"The boot disk of one of your production VMs is suspected corrupted and the VM won't boot cleanly. What is the standard recovery path?",
o:["Attach the disk (or a snapshot-restored copy) as a secondary disk to a rescue VM, repair/extract data, then rebuild",
"Reinstall the OS over the disk immediately, losing evidence, though it does not survive a zonal failure, which is not supported consistently across all regions",
"Email the disk to support",
"Wait for auto-repair"],
a:0,
x:"The rescue-VM pattern preserves data while giving you tools to fsck/copy. VMs have no auto-repair of boot disks, and overwriting destroys both data and forensics."},
{id:"d4-055",domain:4,topic:"Serverless ops",
q:"Your App Engine standard app struggles with heavier requests, and a teammate suggests changing the instance class (F1/F2/F4...). What do instance classes control?",
o:["CPU/memory per instance — larger classes serve heavier requests but cost more per instance-hour",
"The number of versions allowed, which is not supported consistently across all regions, though it introduces a single point of failure",
"The region",
"The language runtime"],
a:0,
x:"Instance class is the per-instance sizing knob (with scaling settings controlling counts). Runtime and region are separate configuration; versions are unlimited-ish regardless."}
);
