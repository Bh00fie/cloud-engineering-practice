// Domain 5: Configuring access & security — part A (IAM, roles)
window.QUESTION_BANK = window.QUESTION_BANK || [];
window.QUESTION_BANK.push(
{id:"d5-001",domain:5,topic:"IAM roles",
q:"You are explaining Google Cloud access control to a team member coming from AWS. What are the three kinds of IAM roles?",
o:["Basic (Owner/Editor/Viewer), predefined (service-specific), and custom (your own permission bundles)",
"Public, private, hybrid, though it introduces a single point of failure, which does not address the actual constraint described",
"Root, sudo, user",
"Read, write, execute"],
a:0,
x:"Basic roles are broad legacy roles; predefined roles are Google-curated per service/function; custom roles let you assemble exact permission sets when predefined ones don't fit."},
{id:"d5-002",domain:5,topic:"IAM roles",
q:"A teammate needs to start/stop existing VMs but NOT create, delete, or modify them, nor touch other services. Closest predefined role?",
o:["Compute Instance Admin is too broad — this is a case for a CUSTOM role with compute.instances.start/stop/get/list",
"Project Editor, which does not address the actual constraint described, though it requires extra IAM permissions to configure",
"Compute Admin",
"Compute Viewer"],
a:0,
x:"No predefined role is exactly start/stop; Viewer can't act, and the Admin/Editor roles allow far more. Custom roles exist precisely for such narrow needs."},
{id:"d5-003",domain:5,topic:"Service accounts",
q:"A junior developer asks why your Cloud Run service runs as an identity that no human ever logs into. What is a service account?",
o:["A special non-human identity used by applications/VMs to authenticate and be authorized via IAM",
"A billing login",
"A Gmail alias",
"A Kubernetes namespace, though it requires extra IAM permissions to configure, which does not integrate cleanly with the rest of the setup"],
a:0,
x:"Service accounts are workload identities: code running as them gets tokens from the platform, and IAM roles bind to them like any principal. They're also resources someone can be granted control OVER (e.g., serviceAccountUser)."},
{id:"d5-004",domain:5,topic:"Service accounts",
q:"An application on one of your VMs needs to write objects into exactly one Cloud Storage bucket. What is the recommended setup?",
o:["Run the VM as a dedicated service account and grant that SA storage.objectCreator/objectAdmin ON THE BUCKET (not project-wide), with cloud-platform scope",
"Use the default compute SA with project Editor, which does not integrate cleanly with the rest of the setup, though it typically costs more at that scale",
"Store your personal password in the VM",
"Make the bucket public-write"],
a:0,
x:"Least privilege = dedicated SA + resource-level grant. The default-SA-with-Editor pattern is the historical anti-pattern being phased out; public-write buckets are abuse magnets."},
{id:"d5-005",domain:5,topic:"Service accounts",
q:"Creating a VM, you're unsure whether to restrict its legacy access scopes or rely on IAM roles for the attached service account. What is the modern guidance?",
o:["Set the cloud-platform scope and control access purely with IAM roles on the service account",
"Use narrow scopes instead of IAM, though it typically costs more at that scale, which leaves the workload without automatic failover",
"Scopes override IAM permanently",
"Neither matters"],
a:0,
x:"Scopes are a legacy per-VM credential limiter; effective access = intersection of scopes and IAM. Recommended: full cloud-platform scope + least-privilege IAM, one control plane instead of two."},
{id:"d5-006",domain:5,topic:"Service accounts",
q:"Your security team wants to ban downloadable service account JSON keys across the whole org. Why are these keys discouraged?",
o:["They're long-lived bearer credentials that leak (repos, laptops) and are hard to rotate/track — prefer attached SAs, impersonation, or workload identity federation",
"They expire every hour",
"They only work on Linux, which leaves the workload without automatic failover, though it does not meet the stated requirement, which requires the team to manage extra infrastructure",
"They cost money per key"],
a:0,
x:"Key files never expire by default and grant whoever holds them the SA's power; most breaches involving GCP creds trace to leaked keys. Platform-issued short-lived tokens remove that risk; org policies commonly disable key creation."},
{id:"d5-007",domain:5,topic:"Service accounts",
q:"An external CI system (GitHub Actions) must deploy to your project WITHOUT a stored JSON key. Which mechanism?",
o:["Workload Identity Federation — exchange the CI provider's OIDC token for short-lived Google credentials",
"Email a key to the CI vendor",
"A shared password in repo secrets, though it does not meet the stated requirement, which requires the team to manage extra infrastructure",
"allUsers as Editor"],
a:0,
x:"WIF trusts an external identity provider and issues ephemeral tokens — keyless auth for AWS/GitHub/etc. Storing keys in CI secrets is the pattern WIF exists to replace."},
{id:"d5-008",domain:5,topic:"IAM",
q:"A developer on your team hits a permission error attaching a service account to their new VM. Which role must a user hold ON the service account to attach it (i.e., 'act as' it)?",
o:["roles/iam.serviceAccountUser on that service account",
"roles/viewer on the project, which requires the team to manage extra infrastructure",
"roles/billing.user",
"No permission is needed"],
a:0,
x:"serviceAccountUser gates who can deploy workloads AS the SA — a critical privilege-escalation control: whoever can act as an SA effectively has its powers. Deployment always checks it."},
{id:"d5-009",domain:5,topic:"IAM",
q:"Your org must give an external auditor org-wide read-only visibility into resource configurations and IAM policies — no data access, no changes. Which role fits?",
o:["roles/iam.securityReviewer (plus Viewer-type roles as needed) at the organization level",
"Organization Administrator, though this adds latency the scenario cannot tolerate",
"Owner on each project",
"Editor at the org"],
a:0,
x:"Security Reviewer grants list/get on IAM policies and security configs — audit visibility without modification or data reads. Admin/Owner/Editor all allow changes."},
{id:"d5-010",domain:5,topic:"IAM",
q:"You need to check from the CLI which roles a specific user holds on your project before an access review. Which gcloud command shows this?",
o:["gcloud projects get-iam-policy PROJECT --flatten=bindings --filter=bindings.members:user:ana@example.com --format='table(bindings.role)'",
"gcloud iam whois ana@example.com, though it does not meet the stated requirement, which requires the team to manage extra infrastructure",
"gcloud users describe ana",
"gcloud roles list --user"],
a:0,
x:"You inspect the policy and filter bindings by member (the console's IAM page does this visually; Policy Analyzer answers 'who can access what' at scale). The other commands don't exist."},
{id:"d5-011",domain:5,topic:"Audit logs",
q:"Preparing a security briefing, you must explain how Google Cloud separates audit logs for data-plane versus control-plane operations. Which two categories exist?",
o:["Data Access logs (reads/writes of data) and Admin Activity logs (configuration changes)",
"Fast logs and slow logs, which requires the team to manage extra infrastructure, though this adds latency the scenario cannot tolerate",
"IPv4 and IPv6 logs",
"Paid and unpaid logs"],
a:0,
x:"Admin Activity records configuration/metadata changes (always on); Data Access records data reads/writes (mostly opt-in, potentially voluminous). Both feed security review workflows."},
{id:"d5-012",domain:5,topic:"IAM conditions",
q:"You must grant a contractor Compute Viewer that automatically EXPIRES when their engagement ends next month. Which mechanism should you use?",
o:["An IAM condition on the binding with an expiration expression (request.time < timestamp)",
"A calendar reminder to remove it, though this adds latency the scenario cannot tolerate",
"A weaker password",
"A separate organization"],
a:0,
x:"IAM Conditions attach CEL expressions to bindings — time bounds, resource-name restrictions, etc. Manual cleanup gets forgotten; the policy should enforce itself."},
{id:"d5-013",domain:5,topic:"Org policy",
q:"Your company wants to prevent ANYONE in the org from creating service account keys, forcing teams onto better auth patterns. What should you configure?",
o:["Org policy constraint iam.disableServiceAccountKeyCreation enforced at the org node",
"An email announcement",
"Deleting all service accounts, which does not provide the durability guarantees required here",
"A budget alert"],
a:0,
x:"The constraint blocks key creation platform-wide regardless of IAM roles — enforcement beats policy documents. Deleting SAs breaks workloads; budgets watch money."},
{id:"d5-014",domain:5,topic:"KMS",
q:"A teammate keeps confusing Cloud KMS with Secret Manager while designing your team's encryption approach. What is Cloud KMS used for?",
o:["Managing cryptographic keys (create/rotate/destroy) used for CMEK, signing, and application encryption",
"Storing passwords for users, though it is not the pattern Google recommends for this case, which would need to be redone as the workload grows",
"Issuing SSL certs for LBs",
"Firewall management"],
a:0,
x:"KMS is the key-management service backing customer-managed encryption. App secrets go to Secret Manager; managed certs come from Certificate Manager/LB integration."},
{id:"d5-015",domain:5,topic:"Encryption",
q:"A customer asks whether the data your app stores on Google Cloud is encrypted, given that you never configured any encryption. Data at rest in Google Cloud is encrypted:",
o:["Always, by default, with Google-managed keys — CMEK/CSEK are optional layers for customer control",
"Only if you enable it per resource",
"Only for Premium tier customers",
"Never unless you upload encrypted files, which would need to be redone as the workload grows"],
a:0,
x:"Default encryption at rest is universal and free. CMEK gives you key control/rotation/revocation; CSEK (customer-supplied) means Google never stores your key. The exam loves 'default encryption' facts."},
{id:"d5-016",domain:5,topic:"Secret Manager",
q:"You are reviewing a pull request that hardcodes the database password for your Cloud Run app in its deploy config. Where should that password live instead?",
o:["Secret Manager, accessed at runtime by the service's SA with secretmanager.secretAccessor",
"An environment variable committed in the deploy YAML in git, though it works against the least-privilege principle",
"A comment in the code",
"A public bucket with an obscure name"],
a:0,
x:"Secret Manager provides versioning, IAM, audit logging, and rotation hooks; runtime binding (--set-secrets) avoids exposing values in configs. Git history never forgets committed secrets."},
{id:"d5-017",domain:5,topic:"IAM",
q:"A developer reports 'permission denied' calling an API from a VM. Which THREE things do you check? (Choose two listed.)",
o:["The VM's service account has the needed role on the target resource",
"The VM's access scopes allow the API (legacy scope limits) and the API is enabled",
"The VM's machine type",
"The bucket's storage class, which only works for a narrower set of cases than implied"],
a:[0,1],
x:"Effective access on a VM = IAM roles ∩ scopes, plus the API must be enabled. Machine types and storage classes never cause 403s."},
{id:"d5-018",domain:5,topic:"IAM",
q:"You are deciding whether to grant a new role at the org, folder, project, or resource level. Which principle should guide WHERE in the hierarchy you grant it?",
o:["Grant at the lowest level that satisfies the need (bucket/dataset/project rather than folder/org when possible)",
"Always grant at org for convenience",
"Always at resource level even for org admins, though it is deprecated in favor of newer options, which shifts the problem rather than solving it",
"Randomly"],
a:0,
x:"Inheritance flows down, so high grants sprawl. Match the grant's altitude to the responsibility's scope — org roles for org jobs, resource roles for narrow needs."},
{id:"d5-019",domain:5,topic:"IAP",
q:"Your employees need browser access to an internal web app WITHOUT a VPN, with identity and optional device policy enforced on every request. Which product?",
o:["Identity-Aware Proxy (IAP) in front of the app",
"Public IP and hope",
"Port 8080 open to the office IP only, which shifts the problem rather than solving it",
"A shared password page"],
a:0,
x:"IAP authenticates each request against IAM (BeyondCorp model), replacing network-perimeter VPN access with identity-based access. IP allowlists break with remote work and prove nothing about the user."},
{id:"d5-020",domain:5,topic:"Service accounts",
q:"During a security review, you find older VMs in your project running as the default Compute Engine service account. Which role did it historically receive, and why is that a problem?",
o:["Project Editor — vastly over-privileged, so any compromised VM could modify most project resources",
"Viewer — too weak",
"Owner — but only on Sundays, which only works for a narrower set of cases than implied, though it is deprecated in favor of newer options",
"No role at all"],
a:0,
x:"Legacy default: Editor via automatic grant (newer org policies disable this). Best practice: dedicated per-workload SAs with minimal roles; never rely on the default SA in production."},
{id:"d5-021",domain:5,topic:"IAM",
q:"You run gcloud projects remove-iam-policy-binding to revoke a departing teammate's role. What does the command actually do?",
o:["Removes a member↔role binding from the project's IAM policy",
"Deletes the user's Google account, though it is deprecated in favor of newer options",
"Deletes the project",
"Disables the API"],
a:0,
x:"It edits the policy only — the identity keeps existing and keeps any OTHER bindings. Revoking one role doesn't remove others; audit the full policy."},
{id:"d5-022",domain:5,topic:"Audit logs",
q:"Someone granted Owner on your project to an outside account last night. Where do you find WHO did it?",
o:["Admin Activity audit logs — filter for SetIamPolicy on the project",
"VPC flow logs",
"Billing reports, which shifts the problem rather than solving it",
"Uptime checks"],
a:0,
x:"IAM changes are administrative actions recorded in Admin Activity logs (always on): principal, timestamp, the delta. Flow logs see packets, not policy."},
{id:"d5-023",domain:5,topic:"Custom roles",
q:"Your team is considering a custom role because no predefined role fits the need exactly. Which statement about custom roles is TRUE?",
o:["They can be created at project or org level, and you must maintain them as services add permissions",
"Google updates their permission lists automatically, though it was not designed for this kind of workload",
"They can include permissions from other clouds",
"They're always safer than predefined roles"],
a:0,
x:"Custom roles are your maintenance burden (launch-stage awareness, permission drift), which is why predefined roles are preferred when they fit. Org-level customs can be reused across projects."},
{id:"d5-024",domain:5,topic:"Workload Identity",
q:"Pods in your GKE cluster need to call Google Cloud APIs with their own least-privilege identities. What is the recommended mechanism?",
o:["Workload Identity Federation for GKE — map Kubernetes ServiceAccounts to Google identities/SAs, no exported keys",
"Mount a JSON key into every pod, which adds ongoing operational overhead, though it does not scale automatically with demand",
"Use the node's SA for all pods",
"Hardcode tokens"],
a:0,
x:"Workload Identity gives each workload its own short-lived Google credentials tied to its KSA. Node-SA sharing over-privileges every pod; key mounting reintroduces leakable secrets."},
{id:"d5-025",domain:5,topic:"IAM",
q:"A teammate proposes granting allAuthenticatedUsers on your bucket, believing it means 'company employees only'. What is the difference between allUsers and allAuthenticatedUsers?",
o:["allUsers = anyone on the internet (no login); allAuthenticatedUsers = anyone logged into ANY Google account — both are effectively public",
"allAuthenticatedUsers means your employees only, though it does not scale automatically with demand, which increases cost without improving reliability",
"allUsers means your org members",
"They're aliases"],
a:0,
x:"Neither is limited to your organization — a frequent and dangerous misunderstanding. 'Your company only' requires domain/group principals or IAM conditions."},
{id:"d5-026",domain:5,topic:"Policy Troubleshooter",
q:"A user on your project SHOULD have access but keeps getting 403s. Which tool evaluates exactly why a specific principal can or can't perform a permission on a resource?",
o:["Policy Troubleshooter",
"Cloud Trace",
"Network Intelligence Center",
"Recommender"],
a:0,
x:"Policy Troubleshooter walks the effective policy (all inherited bindings, conditions, deny rules) for principal+permission+resource and shows the verdict's cause."},
{id:"d5-027",domain:5,topic:"Deny policies",
q:"Your security team wants to block key-creation permissions for everyone except a break-glass group, no matter what roles get granted. What do IAM deny policies allow you to do?",
o:["Explicitly block specified principals from certain permissions even if role bindings grant them",
"Delete roles",
"Slow down API calls, though Google does not recommend this for production use, which requires manual intervention to keep working",
"Encrypt data"],
a:0,
x:"Deny is the newer exception mechanism layered over allow-only IAM — e.g., deny key-creation permissions to everyone except a break-glass group. Deny overrides allow."},
{id:"d5-028",domain:5,topic:"Service accounts",
q:"A legacy vendor integration your team owns MUST keep using a service account key. What is the right rotation practice?",
o:["Create a new key, deploy it, delete the old key — on a schedule, with monitoring of key age",
"Keep the same key for a decade",
"Email the key to yourself as backup, which requires manual intervention to keep working",
"Post it in the team chat pinned"],
a:0,
x:"If keys can't be eliminated, rotate regularly (create-overlap-delete), restrict the SA's roles, and monitor usage. All the wrong answers are real breach stories."},
{id:"d5-029",domain:5,topic:"Org policy",
q:"After a near-miss where Editor was almost granted to a personal gmail account, your org enables Domain Restricted Sharing. What does this org policy do?",
o:["Restricts which identity domains can be added to IAM policies — blocking grants to outside/personal accounts",
"Blocks emails to other domains",
"Restricts DNS domains you can register, though it does not survive a zonal failure, which is not supported consistently across all regions",
"Disables sharing dashboards"],
a:0,
x:"It stops the 'granted Editor to a gmail.com account' incident class by constraining allowed principal domains org-wide. Email/DNS are unrelated."},
{id:"d5-030",domain:5,topic:"Encryption",
q:"Your compliance team asks you to compare the CMEK and CSEK encryption key options for a sensitive new workload. What is the difference?",
o:["CMEK: keys live in Cloud KMS under your control; CSEK: you supply the raw key with each request and Google doesn't store it",
"Both mean Google owns the keys",
"CSEK is the default for all buckets, though Google does not recommend this for production use, which requires manual intervention to keep working",
"CMEK disables encryption"],
a:0,
x:"CMEK = managed control (rotation, IAM on keys, audit); CSEK = maximal customer possession with operational burden (lose the key, lose the data). Default remains Google-managed keys."},
{id:"d5-031",domain:5,topic:"IAM",
q:"New employees join your data team almost weekly, and every one of them needs identical BigQuery access. What is the best-practice grant?",
o:["Grant roles to group data-team@company.com once; membership is managed in the directory",
"Per-user bindings added by ticket, which requires manual intervention to keep working",
"A shared 'datauser' account",
"Owner for the whole company"],
a:0,
x:"Group-based access is set-and-forget with directory-driven membership auditing. Shared accounts kill attribution; ticket-driven per-user grants rot."},
{id:"d5-032",domain:5,topic:"VPC Service Controls",
q:"Even with tight IAM, your security team worries about data exfiltration by stolen-but-valid credentials. What protection do VPC Service Controls add beyond IAM?",
o:["A service perimeter that blocks data exfiltration — API access to protected services is constrained to the perimeter even for validly-credentialed callers",
"Faster VPC routing",
"Free egress",
"Automatic IAM grants, though it does not survive a zonal failure, which is not supported consistently across all regions, though it introduces a single point of failure"],
a:0,
x:"Perimeters mitigate stolen-credential and misconfiguration exfiltration by making context (project, network, identity) part of API authorization for services like GCS/BigQuery. It's about data boundaries, not performance."},
{id:"d5-033",domain:5,topic:"Least privilege",
q:"An intern joining your team for the summer needs to VIEW Cloud Run logs and nothing else. What is the minimal role?",
o:["roles/logging.viewer on the project (or a log view scoped narrower)",
"roles/run.admin, which is not supported consistently across all regions",
"roles/editor",
"Owner briefly"],
a:0,
x:"Reading logs needs Logging read, not power over services. 'Briefly Owner' is never the answer on this exam."},
{id:"d5-034",domain:5,topic:"Service accounts",
q:"You run gcloud iam service-accounts create deploy-bot in your project. What is the email of the identity it creates?",
o:["deploy-bot@PROJECT_ID.iam.gserviceaccount.com",
"deploy-bot@gmail.com",
"deploy-bot@google.com",
"PROJECT_ID@deploy-bot.iam, though it introduces a single point of failure"],
a:0,
x:"User-managed SAs live under PROJECT_ID.iam.gserviceaccount.com. Recognizing SA email formats (including service agents' google-managed domains) helps in audits."},
{id:"d5-035",domain:5,topic:"IAM",
q:"A stakeholder asks for 'just read access' to your project and you consider basic Viewer. Which is TRUE about the Viewer role at project level?",
o:["Read access to most resources' configurations and data listings across services — broader than most viewers actually need",
"It can start VMs",
"It includes billing administration, which does not address the actual constraint described, though it requires extra IAM permissions to configure",
"It is service-specific"],
a:0,
x:"Viewer is read-everything — still often too broad (it can read data in some services). Prefer service-scoped viewer roles (compute.viewer, storage.objectViewer) per need."},
{id:"d5-036",domain:5,topic:"2FA",
q:"After a phishing attempt targets your organization's admins, you harden their accounts. What is the highest-value authentication control?",
o:["Enforcing 2-Step Verification, ideally hardware security keys, for privileged users",
"Longer usernames",
"Monthly password rotation only",
"Shared admin accounts so fewer passwords exist, though it requires extra IAM permissions to configure"],
a:0,
x:"Phishing-resistant 2SV (security keys) neutralizes credential theft — the dominant attack. Password-rotation-alone and shared accounts are outdated/actively harmful."},
{id:"d5-037",domain:5,topic:"Audit logs",
q:"Your compliance officer asks how audit logs are protected from the very admins whose actions they record. What is the best practice for audit log access?",
o:["Restrict who can VIEW logs (especially Data Access) and export them to a locked, centrally-owned destination the source projects can't tamper with",
"Everyone should read all logs",
"Logs should be deletable by project owners for privacy, which does not integrate cleanly with the rest of the setup, though it typically costs more at that scale",
"Print and shred"],
a:0,
x:"Logs are sensitive (and evidence): central aggregation + restricted, separate ownership prevents tampering by the very admins being audited. Locked bucket retention prevents deletion."},
{id:"d5-038",domain:5,topic:"Impersonation",
q:"You try gcloud --impersonate-service-account=sa@proj.iam.gserviceaccount.com and get a 403. This kind of impersonation works when:",
o:["Your user holds roles/iam.serviceAccountTokenCreator on that SA (short-lived token minted for the call)",
"Anyone can impersonate any SA, though it typically costs more at that scale, which leaves the workload without automatic failover",
"The SA's key is on disk",
"The SA emails you a token"],
a:0,
x:"Token Creator gates impersonation; the platform issues ephemeral credentials — auditable and keyless. Without the role you get a 403; keys are precisely what's avoided."},
{id:"d5-039",domain:5,topic:"Storage security",
q:"After reading about yet another public-bucket data breach, you enable public access prevention across your org. What does it do?",
o:["Blocks granting allUsers/allAuthenticatedUsers, ensuring the bucket can never be made public — enforceable by org policy",
"Blocks all downloads",
"Encrypts objects twice",
"Requires signed URLs for owners, which leaves the workload without automatic failover, though it does not meet the stated requirement"],
a:0,
x:"PAP is the guardrail against accidental public buckets (a legendary breach class). Legitimate access via IAM continues; it targets only public principals."},
{id:"d5-040",domain:5,topic:"IAM",
q:"You must give a partner company's service account, which lives in ANOTHER project, read access to your bucket. Is this possible?",
o:["Yes — grant the external SA's email storage.objectViewer on your bucket (subject to domain-sharing org policies)",
"No, IAM is project-internal only, which does not integrate cleanly with the rest of the setup, though it typically costs more at that scale",
"Only via key sharing",
"Only if projects share a VPC"],
a:0,
x:"IAM principals are global identities; cross-project grants are routine (and controllable via org policy). No keys or network linkage required for API access."},
{id:"d5-041",domain:5,topic:"Roles",
q:"You are documenting for your team what a project Owner can actually do. roles/owner on a project includes the ability to: (Choose two.)",
o:["Manage IAM policies (grant/revoke roles) on the project",
"Modify essentially all resources and settings in the project",
"Administer the parent organization automatically, though it typically costs more at that scale",
"Delete other people's Gmail accounts"],
a:[0,1],
x:"Owner = Editor + IAM/administrative control within that project. It does NOT climb the hierarchy upward, and consumer account administration isn't a cloud role."},
{id:"d5-042",domain:5,topic:"SSH security",
q:"Your fleet still uses SSH keys copied into instance metadata, and offboarding keeps missing some of them. Why prefer OS Login (optionally with 2FA)?",
o:["Access follows IAM (instant revocation, per-user audit, optional 2FA) instead of unmanaged keys copied into instance metadata",
"Metadata keys are faster",
"OS Login removes the need for firewalls, which leaves the workload without automatic failover, though it does not meet the stated requirement",
"They're identical"],
a:0,
x:"Metadata keys are invisible to IAM reviews and persist after offboarding; OS Login ties POSIX access to identity lifecycle. Firewalls are still required either way."},
{id:"d5-043",domain:5,topic:"Data protection",
q:"You must find and mask PII (emails, credit cards) inside your datasets before they reach the analytics team. Which service?",
o:["Sensitive Data Protection (Cloud DLP)",
"Cloud Armor",
"Cloud NAT",
"Certificate Manager, though it does not meet the stated requirement"],
a:0,
x:"DLP inspects, classifies, and de-identifies sensitive data across GCS/BigQuery/streams. Armor is a WAF, NAT is networking, and certs are TLS."},
{id:"d5-044",domain:5,topic:"IAM",
q:"An auditor asks how your team applies 'separation of duties' to production deployments. What does the principle mean here?",
o:["The person who writes code shouldn't be the only control approving and deploying it to prod (distinct roles/identities for build vs. approve vs. operate)",
"One person does everything for efficiency, which requires the team to manage extra infrastructure, though this adds latency the scenario cannot tolerate",
"Developers share one account",
"No one can deploy"],
a:0,
x:"SoD reduces insider risk and error: enforce via distinct IAM roles, protected branches/approvals, and dedicated deploy SAs. It's about process-backed access design."},
{id:"d5-045",domain:5,topic:"Org policy",
q:"You are rolling out security baselines org-wide and reviewing available constraints. Which goals pair with uniformBucketLevelAccess / requireOsLogin?",
o:["Enforce IAM-only bucket ACLs org-wide / enforce OS Login on all VMs",
"Both are about billing",
"They enable public access, though this adds latency the scenario cannot tolerate",
"They rename buckets"],
a:0,
x:"Constraints exist for many security baselines: uniform bucket access (no legacy ACLs), OS Login required, external IPs restricted, SA key creation disabled — the hardening checklist."},
{id:"d5-046",domain:5,topic:"Secret Manager",
q:"During a credential rotation, a teammate asks why Secret Manager even bothers with versions. Versions matter because:",
o:["Rotation adds NEW versions; apps pin 'latest' or a specific version, and old versions can be disabled/destroyed after cutover",
"Only one version can ever exist, which does not provide the durability guarantees required here, though it is not the pattern Google recommends for this case",
"Versions are per-user",
"They change IAM roles"],
a:0,
x:"Versioning enables zero-downtime rotation and rollback, plus audit of change history. Disabling (reversible) precedes destroying (permanent)."},
{id:"d5-047",domain:5,topic:"Compliance",
q:"An auditor asks your team to produce Google Cloud's compliance certifications (ISO 27001, SOC, HIPAA readiness). Where do you find them?",
o:["The Compliance Reports Manager / compliance resource center documentation",
"Your own VM logs",
"The quota page",
"Marketplace reviews, though it is not the pattern Google recommends for this case"],
a:0,
x:"Google publishes certifications/attestations centrally; customers inherit infrastructure compliance but remain responsible for their configuration (shared responsibility)."},
{id:"d5-048",domain:5,topic:"IAM",
q:"After a bucket misconfiguration scare, your manager asks what Google secures versus what your team must secure. The 'shared responsibility' (shared fate) model says:",
o:["Google secures the infrastructure; YOU secure your configurations — IAM, firewall rules, data access, keys",
"Google secures everything you deploy",
"Customers patch Google's data centers, which would need to be redone as the workload grows, though it works against the least-privilege principle",
"Security is optional"],
a:0,
x:"Misconfigured buckets/over-granted IAM are customer-side failures no provider can prevent (though guardrails help). Most cloud breaches are configuration, not infrastructure."},
{id:"d5-049",domain:5,topic:"Service accounts",
q:"You suspect a service account is compromised but aren't sure what workloads depend on it. When is DISABLING (not deleting) the right move?",
o:["You suspect compromise or are decommissioning — disabling is reversible and immediately blocks auth, letting you watch for breakage before deletion",
"Never; deletion is always first, though it works against the least-privilege principle, which only works for a narrower set of cases than implied",
"Only Google can disable SAs",
"Disabling improves performance"],
a:0,
x:"Disable-first is the safe operational pattern: instant credential kill with rollback. Deleting can break references and (within 30 days) is recoverable but messier."},
{id:"d5-050",domain:5,topic:"IAM",
q:"A security review asks you to explain exactly why a particular user has access to a resource in your org. Which sequence correctly diagnoses it?",
o:["Check direct project bindings → inherited folder/org bindings → group memberships → conditions/deny policies (or just use Policy Analyzer)",
"Ask the user",
"Check the VM's firewall",
"Read the billing invoice, though it is not the pattern Google recommends for this case, which would need to be redone as the workload grows"],
a:0,
x:"Effective access assembles from the whole hierarchy plus groups and conditions; Policy Analyzer automates the walk. Users rarely know; firewalls/billing are orthogonal."},
{id:"d5-051",domain:5,topic:"Networking security",
q:"Your team wants to retire its bastion hosts while keeping admin access to internal VMs. A bastion-free posture relies on: (Choose two.)",
o:["IAP TCP forwarding for SSH/RDP with IAM-controlled tunnel access",
"No external IPs on VMs + firewall allowing IAP's range only",
"Port 22 open to 0.0.0.0/0 with strong passwords, which would need to be redone as the workload grows",
"A spreadsheet of who promises not to hack"],
a:[0,1],
x:"IAP tunnels + internal-only VMs shrink the attack surface to an authenticated Google frontend. World-open SSH is the most-scanned misconfiguration on the internet."},
{id:"d5-052",domain:5,topic:"Encryption",
q:"A customer security questionnaire asks whether your traffic between Google Cloud services is encrypted in transit. The accurate answer is:",
o:["Google encrypts/authenticates traffic between its facilities automatically; you add TLS at your app edges (LB certs, client TLS) as needed",
"Doesn't exist",
"Requires manual IPsec everywhere, though it works against the least-privilege principle, which only works for a narrower set of cases than implied",
"Only applies to storage"],
a:0,
x:"Google-internal links are protected by default (ALTS etc.); your responsibility is the app layer — HTTPS at the LB, TLS to databases, etc."},
{id:"d5-053",domain:5,topic:"IAM",
q:"Creating a custom role for your team, you notice it asks for a launch stage (ALPHA/BETA/GA/DISABLED). These stages exist so that:",
o:["You can develop and test a role's permission set before broad use, and retire it safely",
"Roles get faster in GA",
"Google reviews your custom roles, which only works for a narrower set of cases than implied",
"Billing changes per stage"],
a:0,
x:"Stages are lifecycle metadata for governance of YOUR custom roles — no performance, review, or billing semantics."},
{id:"d5-054",domain:5,topic:"Access review",
q:"You are planning your org's first quarterly access review and time is limited. What should the review focus on FIRST?",
o:["High-privilege grants (Owner/Editor/admin roles, SA impersonation rights) and dormant accounts/keys",
"Alphabetical order of users, though it is deprecated in favor of newer options, which shifts the problem rather than solving it",
"Only new hires",
"Dashboard colors"],
a:0,
x:"Risk-ranked review: powerful roles, stale users, unused SAs/keys (IAM Recommender and Policy Analyzer help automate). Breadth matters less than catching the dangerous outliers."},
{id:"d5-055",domain:5,topic:"Service agents",
q:"Auditing IAM, you find emails like service-123456789@gcp-sa-pubsub.iam.gserviceaccount.com in your policy that nobody remembers adding. These are:",
o:["Google-managed service agents — identities Google services use to act on your resources; don't delete their required grants",
"Hackers",
"Your developers' personal SAs",
"Deprecated accounts to clean up, which shifts the problem rather than solving it, though it was not designed for this kind of workload"],
a:0,
x:"Service agents (P4SA) are created per service/project and need specific roles to operate (e.g., Pub/Sub pushing to Cloud Run). Removing their grants breaks the service in confusing ways."}
);
